MIRRA v6.0 - BUSINESS MODELLING - CONSOLIDATED BRIEF (REDESIGN AS SPINE)
STATUS: DESIGN CONFIRMED 2026-09-24 - REBUILD STEPS 1-4 BUILT (s11) - BLOCKED ON CM-1 FOR
  ASSET-DRIVEN UNITS - NOT YET VERIFIED END TO END
DATE: 2026-09-24 (rewrite of the earlier same-day consolidation; that version treated the
  existing page as the base - wrong. The redesign is the base; existing code is a parts list.)
SUPERSEDES (fold-in, then archive out of project knowledge; repo copies kept):
  S05 Business Modelling build brief / implemented status / stage lock (v5.0, July)
  BUSINESS_MODELLING_NEW_CHAT_HANDOVER_2026-09-14
  BUSINESS_MODELLING_COMPLETE_REBUILD_SCOPING_BRIEF_2026-09-16
  BUSINESS_MODELLING_REBUILD_HANDOVER_2026-09-16
  MIRRA_v5_2_Business_Modelling_Orchestration_and_Page_Flow_Brief_2026-09-16
  MIRRA_v5_2_Business_Outcome_and_Modelling_Concept_Decision_Record_2026-09-16
  BUSINESS_MODELLING_FULL_CIRCLE_ARCHITECTURE_HANDOVER_2026-09-18
NOT READ: S25_..._MODELLING_NEXT_STEPS_v5_1, S30_MODELLING_AND_OUTCOME_COMPLETE_BUILD_BRIEF_v5_1.
KEPT LIVE: BUSINESS_MODELLING_SCENARIO_REDESIGN_DECISION_LOCK_2026-09-12 (Baseline/Upside/Downside).
Numbers are dated evidence, not claims about today's data.

===============================================================================
1. THE DESIGN (owner's flow, confirmed 2026-09-24)
===============================================================================
Business Modelling walks down the same chain the business runs on, top to bottom:
  0. TODAY - the real achieved position from Business Outcome. Read-only. Never mutated by
     modelling. (Real net profit on 2026-09-24 data: -$406,923.)
  1. OVERHEADS - what remains in Cost Summary after direct attribution (vehicle/fuel to assets,
     insurance, finance, and labour on-costs pulled to where they belong). Changing a remaining
     line changes every downstream minimum recoverable rate. First lever because it moves the
     floor for everything below. (Owner's "full circle", 09-18 handover s3.)
  2. LABOUR - per staff type: minimum recoverable rate and charge-out rate. Change the
     charge-out rate (or a genuine on-cost lever); flows into the asset rates that depend on it.
  3. ASSETS and COG MARGIN - asset blended rates and the materials markup, last, because they
     depend on steps 1-2.
  4. RESULT - after each change, net profit = today's real total + the effect of ONLY what was
     touched. Every other source stays exactly at today's achieved figure.
Every source is shown against three markers:
  FLOOR   = breakeven (minimum recoverable rate / breakeven markup)
  START   = current achieved rate from Business Outcome
  CEILING = Best Case: entered rate x the hours it can cover, uncapped by real revenue
            (Business Outcome's "Best Case (100% of Entered Rates & Hours)")
You are always moving a source from its START toward its CEILING and seeing how far above the
FLOOR it ends. FLOOR / START / CEILING as the frame for this page is the owner's decision
2026-09-24; it is not written in any earlier document.
LIVE NET PROFIT (owner requirement 2026-09-24): as any number changes, the effect on net profit
is visible. PROPOSED (open UI choice): keep TODAY fixed and show MODELLED and the CHANGE beside it
(e.g. Today -$407k -> Modelled -$291k, +$116k). Alternative: move the headline itself.
Method: use the EXISTING calculators through injection points (owner: that is why the injection
points were built). Business Modelling owns state, not business calculations.

===============================================================================
2. PRINCIPLES KEPT
===============================================================================
- Owns state and orchestration, not business calculations; reuses the existing owners.
- Baseline (today) is never mutated. With no changes, modelled == today.
- Revenue, net profit and cash are consequences, never levers.
- Levers are genuine decisions: charge-out rates, markup, discretionary overhead lines, training/
  PPE/allowances, staffing/hours, financing. NEVER levers: ACC rate, employer KiwiSaver, ESCT,
  insurance premiums, market fuel prices (acc_manual_override corrects a real rate; not a lever).
- Locked boundaries (S05): do not overwrite source modules or override Model Readiness; do not
  infer or patch missing source truth.
- Copilot briefs (09-16): concept only; their 8-phase effort estimate was not adopted.

===============================================================================
3. FLOOR / START / CEILING - EVIDENCE (dated 2026-09-24, live, real revenue)
===============================================================================
Per source: floor = breakeven rate; ceiling = entered rate; hours = covered hours.
  Foreman        floor $110.72   entered $97.50    2,828 h
  2inc Line      floor $112.51   entered $144.25   1,414 h
  Site Crew      floor $84.65    entered $75.00    1,414 h
  PC55           floor $98.74    entered $190.00   1,200 h
  Materials/COG  floor 0% markup, entered 15% on COGS $1,240,091
  PC15, 2inc Line Pump 2, TEST: 0 covered hours (BO-6) - earn nothing until labour covers them;
    show as flagged, not as a range.
Read-outs: real net profit -$406,923 on $1,572,473 revenue; breakeven revenue $1,979,396.
Note: Foreman and Site Crew have a ceiling BELOW their floor (entered rate under breakeven), so
"ceiling" means "what current entered rates give if every hour is billed", not a limit on the
lever. The page must say so or it will read as a cap.
Whole-business ceiling: take Business Outcome's own Best Case figure. The old lever card's
+$141,391 (independent engine, no levers) is NOT confirmed to equal it - summing the entered-rate
ceilings above gives about +$260k against real cost, leaving a ~$119k gap unexplained (possibly
the non-revenue-bearing cost subtraction added 09-17). Verify before relying on either.

===============================================================================
4. INJECTION POINTS - WHAT EXISTS, WHAT DOES NOT (audit + 09-16 discovery)
===============================================================================
EXISTS (end of the chain): useBusinessOutcomePerSourceRevenue overrides -
  labour_charge_out_rate_overrides, asset_blended_rate_overrides, materials_markup_percent_override
  (feeds effective_materials_markup_percent). resolveLeverOverrides (businessModellingLeverSelectors)
  bridges working-unit levers into that shape; asset-only and single-labour-type units map
  cleanly, mixed/multi-type units are flagged unsupported. useBusinessModelling calls the hook a
  second time -> modelled_per_source. Live 2026-09-24, no levers: real == modelled in View A
  (-406,922.98) and View B (-406,922.97).
PARTIAL: useCostAllocation(inputs) accepts recovery_summary only. useLabour({pnl_recovery_inputs})
  is recovery context, not a wage/ACC override.
DOES NOT EXIST: injection for Cost Summary remaining-overhead lines (zero lever support);
  Rate Builder, Assets, General Overheads, Employee Overheads, Labour on-cost hooks take zero
  parameters. All underlying calculations are pure; only the hook layer lacks injection.
  General Overheads already tracks each P&L line with a category; a lever needs an is_lever flag
  (data addition, not new architecture). Employee Overheads (training/PPE/allowances) are genuine
  lever candidates.
NOT YET PROVEN: that the modelled_per_source rerun MOVES when a lever changes. (The 09-24 console
  test of this did not return the DEBUG text - snippet bug. Re-test.)

===============================================================================
5. THE BEHAVIOUR RULE AND EVIDENCE
===============================================================================
Rule (owner, agreed 09-18, reconfirmed 09-24): touch one lever -> recalculate ONLY that source
from its real cost and the new figure; every other source stays at today's real achieved figure;
total = today's real total + the effect of the change.
Why: (Attempt 1, 09-18) View B real-capacity cascade always reconciles to real revenue, so the
total cannot move whatever the lever - it only re-splits who is carried. (Attempt 2) The independent
engine summed every unit's own uncapped claim, so untouched units jumped to standalone ceilings.
Both conclusions pre-date the 09-23 Business Outcome changes (covered-hours cap, F8). Attempt 2's
dominant distortion (PC15 ~$295k) is gone - PC15 now has 0 covered hours.
Evidence 2026-09-24: the existing per-lever maths is exactly additive. With Materials at 5%
(from 15%) on COGS $1,240,091 = -$124,009 and Site Crew +$5/h x 1,414.12 h = +$7,071, the lever
card headline moved by -$116,938 = the sum of the two; nothing else moved. So "effect of one
change" can be computed reliably per source. Whether it should be computed by the existing
calculators via overrides (preferred, per owner) or by that lever maths is a build decision - to
be settled by the re-test in s4 (NOT YET PROVEN).

===============================================================================
6. EXISTING CODE = PARTS LIST (audited 2026-09-24; dispositions are PROPOSED)
===============================================================================
hooks/useBusinessModelling.js (130)  slim orchestrator; second per-source call -> modelled_per_source.
hooks/useBusinessModellingScenario.js (114) + ScenarioSection + scenario calculations/storage -
  Baseline/Upside/Downside. Baseline is built by buildLiveLeverHeadline with EMPTY levers (the
  independent engine) - live evidence: Baseline card shows $630,568 labelled "Where the business
  actually is right now" while real = -$406,923; Downside text "$630,568 of profit could be lost"
  is meaningless on a loss; locked 15/09, stale. PROPOSED: rebuild on the achieved START
  (owner's 09-16 decision D4 was to use headline_real_capacity), or retire.
hooks/useBusinessModellingLegacyScenario.js (502) + legacy cards - "Previous scenario tools";
  retirement stays a separate deliberate decision.
components: LeverCard (317; props: headline, real_headline, breakeven_summary, lever_rows,
  rate_target_by_group_id, proportional_suggestions, materials_lever_row, materials_markup_percent)
  - per-source floor/current/target maths is reusable; needs re-framing to floor/start/ceiling.
  RealStateCard (76) - natural "TODAY" header. DEBUG block on the page - REMOVE before close.
lib: businessModellingLeverSelectors (348; buildLiveLeverHeadline, buildBreakevenSummary,
  buildGroupLeverRows, buildMaterialsLeverRow, buildProportionalSuggestions, build_materials_input,
  resolveLeverOverrides, buildRealEngineLeverHeadline [exists, unused-status unverified]),
  businessModellingIndependentCalculations (265), businessModellingLeverCalculations (269 - not in
  the file map), scenario calculations (102), storage x2, legacy calculations/selectors.
The S05 July output contract (minimum_model, business_modelling_output_contract) no longer exists
  in code - decide whether Dashboard / AI Export need a Business Modelling contract (they read the
  Business Outcome contract today).
business_type is not read by any Modelling file (matches tracker) - log only.

===============================================================================
7. OPEN DECISIONS AND DISCOVERY (in this order)
===============================================================================
Q1  Net profit display: today fixed + modelled + change (proposed) vs move the headline.
Q2  RESOLVED 2026-09-24: the additive per-source model is the model; the override rerun is inert
    (s11) and kept only as a cross-check.
Q3  Overheads step: discovery of where remaining Cost Summary overhead enters the chain
    (costAllocationOverheadPoolBuilder.js build_overhead_pool conserves the total per 09-16 algebra;
    unconfirmed as the injection point) and which lines are levers (is_lever).
Q4  Labour on-cost levers vs fixed facts; confirm the six 09-18 "full circle" checks (fuel/vehicle
    direct attribution; same for insurance/finance; ACC/KiwiSaver from P&L; remaining pool surfaces
    variance; pool distributes with no loss/double count; circle closes to achieved rate) against
    the verified Summaries / Cost Allocation / Labour / Assets briefs; re-trace only what they
    do not prove.
Q5  What happens to Baseline/Upside/Downside, legacy tools, LeverCard headline (s6).
Q6  COG/Materials cascade: does the cascade lean on Materials correctly? Materials/COGS reads $0
    net profit under real capacity on 2026-09-24 data - check.
Q7  Whole-business ceiling: confirm against Business Outcome Best Case (s3 gap).

===============================================================================
8. PARKED (log, do not fix inline)
===============================================================================
BO-8 (overrides do not reach coverage-gap valuation), BO-10 (TEST group), BO-12 (View B breakdown
row), S-H4 (legacy baseline needs re-lock), owner-remuneration setting (future Labour +
Reconciliation session), debt-to-equity scenario context, ~$20 Outcome-vs-Modelling discrepancy
(re-check), legacy tool retirement, business_type wiring.

===============================================================================
9. VERIFICATION PLAN
===============================================================================
V1  DONE 2026-09-24: it does not move (s11).
V2  Ceiling: confirm per-source ceiling and whole-business Best Case against Business Outcome.
V3  Overhead injection discovery (Q3), read-only first; grep every caller of any hook before
    changing it (8b rule); find_field_references / check_field_parity on the affected pairs.
V4  Build in chain order (overheads, labour, assets/COG); after each: identity test (no change ->
    modelled == today) then live figures read from React props/page text.
V5  Cleanup: remove DEBUG block; settle Baseline/RealState/legacy; fix file map s7 (LeverCard and
    independent calculations are no longer "leave untouched") and add businessModellingLeverCalculations.
V6  has_loaded guard check on Modelling hooks; update tracker with dated figures.

===============================================================================
10. DOC DISPOSITION AT CLOSE
===============================================================================
Remove the seven docs above plus the three S05 v5.0 docs from project knowledge; upload this brief
(renamed [VERIFIED] once proven). Keep the kickoff, variable tracker and file map. Repo copies stay.
===============================================================================
11. SESSION 1 RECORD (2026-09-24) - decisions, build, evidence
===============================================================================
DECISIONS
- REBUILD, DON'T PATCH. New hook + components; per_source read-only. Old LeverCard, DEBUG block and
  Baseline/Upside/Downside left in place until step 5.
- Q2: additive per-source model (below) is the model.
- D-VIEW (owner): View A achieved figures are the base. Materials is floored at cost: START = 0%
  markup, FLOOR 0%, CEILING = Rate Builder markup. View B's negative achieved markup is a View B
  artefact, not used. Owner notes the View A/B question is still discussed; recorded as decided
  so it is not reopened without cause.
MODEL (mechanism; see CALCULATION_MAP_v6_0.txt s10)
- Row achieved revenue = true_cost + non_productive_share + real_capacity_net_profit
  (per_source.labour_groups[].staff[], asset_groups[].assets[]).
- Basis = the unit's COVERED hours (never seat hours). Materials basis = COGS / 100.
- START = achieved / basis; FLOOR = (true_cost + non_productive_share) / basis (full breakeven;
  group_min_rate kept separately); CEILING = entered rate.
- Effect = (target - START) x basis. Modelled = today's real net profit + sum of effects of ONLY
  the rows touched.
- Rows are per working unit x staff type (Business Outcome's own grain). Assets: one row per unit
  (blended rate; S28 per-asset linking not built). Name trap: working unit "Site Crew" holds staff
  type Operator; staff type "Site Crew" sits in PC55 - label every row "unit / staff type".
FILES (new)
  lib/calculations/businessModellingSourceRows.js (buildSourceRows)
  lib/calculations/businessModellingAdditiveModel.js (applyAdditiveLevers)
  lib/storage/businessModellingLeverStorage.js (key qs_tools_business_modelling_levers_v1)
  hooks/useBusinessModellingLevers.js (lazy useState read; has_loaded made redundant - documented)
  components/business-modelling/businessModellingFormat.js, BusinessModellingNetProfitBar.jsx,
    BusinessModellingSourceRow.jsx, BusinessModellingLeverPanel.jsx; page.jsx wired above LeverCard
COMMITS: 7685d66 (step 1); step 2 committed; step 3 commit + step 4 patch (text inputs,
  inputMode decimal, regex filter - number fields drifted on mouse wheel) issued but NOT confirmed.
[VERIFIED 2026-09-24] live, real revenue
  V1: modelled View A/B totals stayed -406,922.98 / -406,922.97 through Site Crew target, Materials
  5% and both; the additive lever card moved -94,745.82, -124,009.07 and -218,754.89 (exactly
  additive). Offline identity tests on the real per_source snapshot: 6/6 PASS (no levers = today;
  own start rate = zero effect; additivity; asset basis = covered hours; reconciliation exact;
  zero-hour rows ignored). Reconciliation: groups' achieved 332,382.26 + materials at cost
  1,240,090.70 = 1,572,472.96 vs real revenue 1,572,472.95.
  Row floors x hours sum exactly to each unit's true cost.
  Lever panel: Site Crew/Operator at $80 = +49,689.90; Materials 5% = +62,004.54; change
  +111,694.43; modelled -295,228.55. Persists across reload; Reset returns to -406,923 / 0.
FOUND - CM-1 (calc map s12): in asset-driven units Business Outcome adds labour's charge rate on
  top of the calculator rate; the owner bills the calculator rate all-in (PC55: calculator rate
  includes the operator). The lever panel's labour rows inside asset-driven units (Senior Operator
  in 2inc Line, Site Crew in PC55) and the asset rows' ceilings are therefore wrong. Fix CM-1 in
  Business Outcome first; then asset-driven units become ONE lever (the all-in unit rate) and their
  labour rows become cost-only.
PENDING (next session, after CM-1)
  UPDATE late 2026-09-24: step 2 = 6a58e70; steps 3-4 = 89bdd1b (confirmed,
    build OK). per_source snapshot committed as
    docs/Audit/v6_0/bm_per_source_2026-09-24.json (a2e8329) - the ps.json
    rename item below is CLOSED.
  Step 5 (decisions): remove old LeverCard (its standalone headline contradicts reality); remove
    DEBUG block; hide Baseline/Upside/Downside behind a collapsed "Previous tools" section
    (recommended - its baseline is the independent engine, not today); fix the pre-existing
    hasMounted set-state-in-effect lint error in page.jsx.
  Open design: per-staff-type lever across units (one rate for Senior Operator everywhere) vs
    per-unit rows. Recommendation: keep per unit, add "set this rate everywhere" later - revisit
    after CM-1, since asset-driven units will no longer carry separate labour levers.
  Audit snapshot docs/Audit/v6_0/ps.json - rename to bm_per_source_2026-09-24.json and commit, or
    delete (may be untracked).
  Overheads step still has no injection point (Q3).
  Close-out: file map s7 (LeverCard / independent calculations no longer "leave untouched"),
    tracker Business Modelling section, V5/V6.

===============================================================================
12. SESSION 2 RECORD (2026-09-25) - CM-1 LOCATION PIVOT + FULL Q&A CLOSE-OUT
===============================================================================
STATUS: design-only session (voice-driven), no code changed. Resolves every
  open question logged in Section 7 except the parts of Q3/Q4 needing an
  actual injection point built (still pending, see below), and adds several
  new decisions not previously logged anywhere. CM-1 (s12 of the calc map)
  also pivoted location - see V6_0_KICKOFF_AND_HANDOVER s8 and
  CALCULATION_MAP_v6_0.txt s12 for that entry; not duplicated here beyond
  the cross-reference below.

Q1 RESOLVED - net profit display: keep TODAY fixed as an anchor, show
  MODELLED alongside it, and show the CHANGE/delta between them (e.g.
  "Today -$407k -> Modelled -$291k, +$116k"). Do not move the headline
  itself. Rationale: keeps the gap still needing to be closed to reach
  breakeven visible at all times.

Q3 + Q4 RESOLVED (merged) - overhead and on-cost levers:
  - Business Modelling only ever touches the UNDISTRIBUTED General Overheads
    pool (the residual left after General Overheads' own distribution runs),
    never an already-distributed line (e.g. fuel already allocated to a
    specific digger) - changing those would corrupt Cost Allocation's
    distribution.
  - Within the undistributed pool, bring the ENTIRE ledger into Business
    Modelling LINE BY LINE (not one blended total) - each line its own
    named, individually adjustable lever.
  - Lock/constrain flags per line: employer contributions (KiwiSaver etc.) =
    hard-locked, never a lever. ACC = constrained/flagged (small legislated
    adjustment band only, not freely open). Everything else in the
    undistributed pool = open lever by default.
  - Labour on-costs / PPE / training: NO separate lever mechanism needed.
    These are not currently broken out per staff type in Labour - they
    already sit inside the undistributed General Overheads pool and are
    automatically covered by the line-by-line mechanism above. (If PPE/
    training are ever broken out per-staff-type in a future Labour change,
    that reopens this as a new, separate question - not now.)
  - Q3's injection-point discovery itself (where remaining Cost Summary
    overhead actually enters the chain, and which lines qualify as
    is_lever) is STILL OPEN/NOT BUILT - this session decided the RULE, not
    the wiring. Remains the first real build task once this module's
    session starts (see brief s9 V3/V4, unchanged).

Q5 RESOLVED - Baseline/Upside/Downside and legacy scenario tools: DELETE
  the old Baseline/Upside/Downside cards and legacy scenario tools from the
  Business Modelling PAGE (not just hide behind a collapsed section, as
  s11's PENDING notes had proposed). Reason: the Baseline card showed a
  contradictory figure (+$630k, from the independent engine with no levers)
  against real net profit (-$407k), actively misleading rather than merely
  stale. The underlying engine
  (lib\calculations\businessModellingIndependentCalculations.js) is NOT
  deleted - it remains the correct, load-bearing engine for the Business
  Outcome health gauge (calculateGaugeInput) and stays on the untouched-list
  in the file map for that reason. Only the Business Modelling PAGE's own
  UI/cards for Baseline/Upside/Downside and the legacy scenario system are
  removed. Recoverable via git history if ever needed again.

Q6 RESOLVED - materials flooring at $0: CONFIRMED CORRECT, not a bug.
  Materials nets to zero markup because the real-capacity cascade has
  already floored it (cannot go negative in View A). This is read as a
  genuine business finding, not a defect: materials markup may be
  cross-subsidising / propping up labour and asset profitability today.
  Materials START = $0, FLOOR = $0, CEILING = whatever Rate Builder's
  markup setting would allow (unchanged from s11's D-VIEW decision).

Q7 RESOLVED - whole-business ceiling: no separate reconciliation of the old
  lever card's ~$141k ceiling against Business Outcome's Best Case is
  needed up front (the s3 "gap" note in this brief is now moot, since the
  old lever card is being deleted per Q5). New approach: whole-business
  ceiling = SUM of each individual source's own per-source ceiling, all
  built from the same real data - verified as a natural side effect of the
  rebuild rather than as a standalone reconciliation task.

NEW DECISION - shortfall distribution method: stays PRO-RATA (every
  profitable source gives up the same percentage of its own profit
  simultaneously), NOT a waterfall (drain the top earner first, then the
  next). Chosen because pro-rata is order-independent and doesn't hide risk
  concentration in the single best-performing source; a waterfall's result
  depends arbitrarily on which source you drain first. This matches the
  cascade's existing Phase 1/2 mechanism (CM-5 in the calc map) - no change
  needed to the mechanism itself, this decision just confirms it's the
  right one to keep going forward.
  OPEN NOTE, carried forward: verify against REAL (not baseline placeholder)
  figures which source is the actual top net-profit driver - PC15 showed
  zero covered hours in the pre-fix baseline (bo_per_source_PRE_CM1_
  2026-09-24.json) so may not currently be it. Confirm the Phase 1 cascade
  treats whichever source it actually is fairly under pro-rata once real,
  current data is checked.

NEW DECISION - lever granularity: BOTH per-staff-type AND per-unit, not
  either/or. Labour rate levers = per staff type (e.g. "Senior Operator" is
  one shared rate that moves every unit that staff type appears in
  simultaneously - it's the real billed rate for that skill level
  regardless of which machine they're on). Asset rate levers = per unit
  (each machine's resulting net rate differs based on its own cost
  structure, by construction of the CM-1 net-asset-rate subtraction).

NEW DECISION - reverse-lever direction: Business Modelling should let the
  user set a TARGET labour rate for a staff type (e.g. bump Senior Operator
  to $85) and show what the total invoiced/calculator rate per affected
  unit would need to become to still leave that labour rate covered, plus
  what's left over for the asset - not just show the effect of changing the
  asset side.

NEW DECISION - multi-line-item calculators: for a unit whose invoice has
  several components (e.g. a setup fee + an hourly rate + a per-cubic-metre
  rate, all rolled into one calculator total today), Business Modelling
  (NOT a Rate Builder rebuild) must surface EACH component as its own
  separately adjustable lever, not just one blended target total. The
  reconciliation/redistribution logic for this belongs in Business
  Modelling.

CROSS-REFERENCE - CM-1 fix location pivoted 2026-09-25 (see
  CALCULATION_MAP_v6_0.txt s12 and V6_0_KICKOFF_AND_HANDOVER_2026-09-20.txt
  s8 for the current, authoritative plan): the calculator-rate-minus-
  labour-rate subtraction moves into Rate Builder, not Business Outcome.
  Once that lands, asset-driven units become ONE lever (the all-in unit
  rate) per s11's PENDING note - unchanged by the pivot, since the pivot
  only moves WHERE the subtraction is calculated, not the resulting lever
  shape Business Modelling presents.

LOGGED, NOT PART OF THIS MODULE - units mode: a full future-build brief was
  written this session, UNITS_MODE_FUTURE_BUILD_BRIEF_2026-09-25.txt
  (docs\fin_mirra_v6.0 Briefs\), covering whether/how this Business
  Modelling floor/start/ceiling design extends to a units (not hours)
  basis. Read that brief in full before ever opening a units-mode session -
  do not re-derive; explicitly out of scope for the current CM-1 / step 9
  work.
