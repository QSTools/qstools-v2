MIRRA v6.0 - KICKOFF & HANDOVER
STATUS: ACTIVE - START HERE FOR ANY V6.0 SESSION
DATE: 2026-09-20 (updated 2026-09-23 after Business Outcome core close-out;
  updated again 2026-09-23 after the 8b follow-up session closed same day;
  updated 2026-09-24 at the close of the first step-9 session - calculation
  map added, CM-1 found, see s2 step 9 and s8;
  updated 2026-09-25 after step-9 session 2 - CM-1 fix location pivoted to
  Rate Builder [not Business Outcome as s8 previously said], full Business
  Modelling Q&A close-out recorded in that brief's own s12, units-mode
  future-build brief written and logged in s7)
SUPERSEDES: all v5.x briefs as the entry point (individual v5.x briefs are
not deleted - see Section 6 for how they get folded in / archived as each
module is reached).

===============================================================================
1. WHY V6.0 EXISTS - READ THIS FIRST
===============================================================================

Business Modelling has stalled. It cannot be trusted until the calculation
chain beneath it is proven correct end to end, starting from Cost
Allocation.

The dependency chain is:

    P&L / Labour / Assets  -->  COST ALLOCATION  -->  Rate Builder
        --> Business Outcome  -->  Business Modelling

Cost Allocation is the hinge. Everything upstream of it just feeds it.
Everything downstream of it (Rate Builder, Business Outcome, Business
Modelling) depends entirely on trusting what it distributes. This pass
exists to verify Cost Allocation is doing exactly what it's supposed to
do - so that everything built on top of it can finally be trusted.

v5.x briefs were written before live-data injection points and Business
Modelling's wiring into Business Outcome existed. Rather than patch old
assumptions, v6.0 starts the verification chain fresh from page one.

DEFINITION OF DONE for this pass: not "every brief is tidy" - done means
any number Business Modelling needs can be traced and trusted, from its
real source through to Cost Allocation's output.

A known, explicitly parked gap to watch throughout: the business_type (labour_based vs.
product_based) setting (set on the intro page) is NOT fully
wired through every downstream module yet. As each module is verified,
explicitly note whether it correctly respects this setting or silently
assumes one. Do not fix this inline unless trivial - log it, keep moving,
fix as a dedicated pass once the gaps are fully known.

A SECOND parked item, added 2026-09-23 during the Business Outcome 8b
session: an owner-remuneration setting (payroll / shareholder salary /
drawings) surfaced while reconciling the P&L cost-gap against Labour's
figures. Logged for a future Labour + Reconciliation session - see the
variable tracker Section 12 and the Business Outcome brief s5 for the
full note. Not fixed inline, per the same rule above.

===============================================================================
2. BUILD PATH (in order)
===============================================================================

Phase 0 - Setup (this document + variable tracker; done 2026-09-20)

Phase 1 - Cross-import alignment check
  Confirm P&L, Balance Sheet, and Revenue/COGS imports agree with each
  other for the same period. No existing check does this today - it's a
  new, standalone check worth building early, before Cost Allocation
  work starts, since nothing downstream can be trusted if the three
  source imports don't agree with each other.

Phase 2 - Sequential module verification (one module per chat session)
  1. Intro page - business type (units vs. hours) - does this setting
     actually flow as real data downstream, and to where?
  2. P&L
  3. Labour module
  4. Assets module
  5. Cost Allocation  <-- the actual milestone this whole pass exists for
  6. Cost Summary / Business Summary / Recovery Summary (added 2026-09-23).
     Upstream of Cost Allocation's overhead pool and cost burden, but only
     verified indirectly via the P2.5 reconciliation. One session, BEFORE
     Rate Builder: write the consolidated Recovery Summary brief (fold in
     A11_RecoverySummary_v6_0_BRIEF_VERIFIED_2026-09-23.txt as its first
     [VERIFIED] entry) and take the Business Summary half of A11, Labour F18,
     Cost Allocation C16, and Recovery Summary R2-R5.
     DONE [VERIFIED 2026-09-23] - Summaries_v6_0_BRIEF_VERIFIED_2026-09-23.txt
     (commits 47ca86d, e517b39, 13f3c58, 51e2e08). Decided: Cost Summary and
     Business Summary are the macro view (per open hour / day / week from
     Opening Hours); Recovery Summary onward is the pricing view (total
     productive person-hours).
  7. Rate Builder. DONE [VERIFIED 2026-09-23, code-traced] -
     RateBuilder_v6_0_BRIEF_VERIFIED_2026-09-23.txt. No code changed.
     Finding that set scope: Rate Builder has no output contract; only its
     stored user inputs (charge rates, calculators) reach Business Outcome.
     Display defects parked. Hand-offs H1-H7 go to Business Outcome.
  8. Business Outcome. CORE DONE [VERIFIED 2026-09-23, live] -
     BusinessOutcome_v6_0_BRIEF_VERIFIED_2026-09-23.txt (commits ab7d721,
     b668bbd). Breakeven held through every change. Decided (BO-6): in an
     asset-driven working unit, revenue comes only from hours assigned labour
     actually covers; a unit with no labour earns nothing and is flagged.
     Verification now reads live figures from the rendered card's React props
     in the browser console (brief s2A) instead of transcribing the screen.
  8b. Business Outcome follow-up. DONE [VERIFIED 2026-09-23, live, SAME DAY
     as the core pass] - commits a401c5e (F1/F3/F4), d4aa80c (F2/F8/BO-9),
     4bdaad4 (F7). All seven planned items (F1-F7) plus one added mid-session
     (F8, BO-11) resolved:
       F1 (S-H2) day/week scaling follows the opening calendar.
       F2 (BO-1) build-up table and ranked drill both show/use the correct,
         non-productive-cost-injected true cost, at parent AND child level.
       F3 (BO-3) child row label -> "Group min rate".
       F4 (BO-4) dead waterfall computation removed from the page.
       F5 breakeven revenue dropped; real revenue restored and re-verified.
       F6 (H5) materials markup path (Rate Builder Path C -> View B) traced;
         also resolves BO-9 (the View B "minimum markup" field is actually
         the ACHIEVED markup, relabelled).
       F7 (BO-2) Labour Recovery card's overhead-per-hour now uses Business
         Outcome's own per-group cost-share split, not Rate Builder's
         all-on-labour basis - user decision: consistency with the rest of
         the page over matching Rate Builder. Same fix applied to the output
         contract hook so AI Export / Dashboard / Business Modelling see the
         same basis.
       F8 (new, BO-11) View B's headline now counts real revenue above what
         its modelled sources explain (a return value that existed in the
         cascade but was never consumed) - user decision: option (a), add it
         to the headline with an explanatory line, over leaving it silently
         excluded.
     One item found but NOT built this session, logged for later: BO-12 -
     View B's own breakdown list doesn't yet carry the new unexplained-
     revenue figure as its own row, so it can visually undercount vs the
     stated headline. Small, self-contained, does not block step 9.
     Full detail, all live numbers, and the three file-map corrections found
     along the way: BusinessOutcome_v6_0_BRIEF_VERIFIED_2026-09-23.txt s5-7,
     BUSINESS_OUTCOME_FILE_MAP_v6_0.txt (both files updated 2026-09-23 8b).
  9. Business Modelling.  <-- IN PROGRESS (2026-09-24)
     CONSOLIDATION DONE 2026-09-24 - BusinessModelling_v6_0_BRIEF_CONSOLIDATED_2026-09-24.txt
     (folds nine v5.x docs; commits 7e76a24 and the redesign rewrite). NOT yet verified.
     Key finding: the existing page is NOT the base. The owner's redesign is the spine:
     overheads > labour rates > assets and COG margin, every source shown against
     FLOOR (breakeven) / START (achieved rate) / CEILING (Best Case), with net profit
     effect visible live. Existing code is a parts list. Overheads have zero lever
     support today. First job next session: brief s9 V1 (re-test whether the override
     rerun moves the total). Also found 2026-09-24: Baseline card contradicts real
     net profit; five uncommitted Business Outcome files were committed (5bacc34).
     SESSION 1 CLOSED 2026-09-24. Done: V1 answered - the override rerun
     (modelled_per_source) does NOT move when a lever changes; the additive
     per-source model is the model (Q2 settled). View A chosen as the base
     (owner). Page rebuild steps 1-4 built: source rows + additive model
     (7685d66), full-breakeven floor + lever storage/hook, lever panel on the
     page (live-verified), text inputs instead of number fields (step 3/4
     commit - CONFIRM in git log at the start of next session). Full record:
     Business Modelling brief s11.
     FOUND: CM-1 - Business Outcome stacks the operator's charge rate ON TOP
     of the asset calculator rate in asset-driven units; the owner bills the
     calculator rate ALL-IN (Rate Builder, Quick Start and the legacy split
     already treat it that way). Overstates every asset-driven unit's
     modelled/Best Case revenue and the coverage gap; real net profit is
     unaffected. Blocks Business Modelling's asset-driven levers.
     BUILT: CALCULATION_MAP_v6_0.txt (docs\Audit\v6_0\) + extract script
     Py\audit-scripts\extract_calc_map.py (v2, with a coverage report).

Phase 3 - Per-module close-out (repeat for each module in Phase 2)
  - Update that module's ONE consolidated brief with [VERIFIED] status,
    date, and real numbers checked.
  - Update the variable tracker with the real, dated figures for that
    stage and the hours/units coverage note.
  - Remove that module's brief from project knowledge; upload the next
    module's brief.
  - Close the chat session. Start a new one for the next module.

===============================================================================
3. TEST DATA APPROACH FOR THIS PASS
===============================================================================

- Tool realigned to match the real P&L and Balance Sheet being used.
- A small number of deliberately added items (some non-productive staff,
  a few extra productive assets) to give Cost Allocation enough real
  variety to test overhead distribution properly. Kept minimal - roughly
  one non-productive staff member and two extra assets.
- CORRECTED 2026-09-23: the P&L and Balance Sheet were NOT updated for
  these additions. The only P&L change is revenue (set to breakeven, below,
  then DROPPED 2026-09-23 in the 8b session - see next bullet). The
  synthetic staff and assets exist in the tool only, so Module
  Reconciliation / Model Readiness is expected to be blocked on this data
  (business cost benchmark reconciliation). Any reconciliation check during
  this phase is checking "does the distribution logic work correctly", not
  "does this match a real, unmodified set of accounts."
- Revenue is set to breakeven (revenue = total true cost, net profit = 0)
  while verifying Cost Allocation specifically. This isolates the cost
  side: Cost Allocation doesn't use revenue, so if headline net profit
  drifts off zero unexpectedly, that's an immediate, obvious signal
  something in the cost distribution didn't reconcile. Breakeven is
  dropped once verification moves on to Rate Builder / Business Outcome,
  which are about revenue and margin and need real figures to test
  properly. DROPPED 2026-09-23 (8b, F5): real revenue (the actual P&L
  Sales figure) restored and every 8b live check re-verified against it.
  A same-day cost-gap reconciliation exercise (see variable tracker
  Section 12, Business Outcome brief s5) fully accounted for the
  previously-unexplained variance between the P&L's labour/interest
  benchmarks and the tool's totals - mostly Owner/Director's own labour
  cost, which surfaced the new owner-remuneration design item (Section 1).
- Once Cost Allocation is proven against this padded-but-consistent
  dataset, decide explicitly whether to strip the synthetic entries back
  out before testing downstream, or carry them through as now-genuinely-
  reconciled data. Note whichever choice is made and why.
  DECIDED at P2.5: carry, empty groups removed (commit 1e5009f).
  Identified 2026-09-23: synthetic assets PC15 and 2inc Line Pump 2;
  synthetic non-productive staff = Accountant (assumed). Mini Truck is real
  and was reclassified to a support asset in its own working unit.
- Breakeven revenue was KEPT through the Business Outcome core pass on
  purpose (headline ~ 0 was the test for every change). Dropped 8b (F5) -
  real revenue used for every 8b live verification from that point on.

===============================================================================
4. PROCESS RULES FOR EVERY V6.0 SESSION
===============================================================================

Full detail: "Working_Conventions - PowerShell Patching, CSS, and Numeric
Verification" (repo root) - this section is a summary, that document is
authoritative.

- One module per chat session. Do not carry v6.0 work for multiple
  modules in one long conversation. (Note: the 8b follow-up session was
  treated as a continuation of the same Business Outcome module, not a new
  module, and was completed the same day as the core pass - this is
  consistent with the rule, since it is still one module.)
- CHANGED 2026-09-24: project knowledge now also holds
  CALCULATION_MAP_v6_0.txt permanently (five documents, not four). It is
  mechanism only, like the tracker.
- CALCULATION CHANGE CHECKLIST (any formula added, removed or changed):
  1. After the patch builds and live-tests, re-run
     python Py\audit-scripts\extract_calc_map.py
  2. Paste back 00_COVERAGE.txt and the changed module's dump section.
  3. Update the matching line(s) in CALCULATION_MAP_v6_0.txt - formula AND
     line numbers (they drift) - and any CM-n entry the change resolves.
  4. Commit the code, the map and the brief entry TOGETHER. Re-upload the
     map to project knowledge at session close.
  A calculation change is not done until the map matches the code.
- WHEN TO LOAD THE CALCULATION MAP (decided per session, stated in s8's
  upload list for the next session):
  LOAD IT for any session that reads, verifies, changes or builds on a
  calculation - module verification, Business Outcome / Business Modelling
  work, any CM-n fix, anything touching revenue, cost, rates or profit.
  LEAVE IT OUT for sessions with no calculation work - CSS consolidation,
  import consolidation scoping, docs-only tidy-ups, UI copy or layout.
  It adds context to every turn, so it only earns its place when a
  formula is in play. If a no-map session hits a calculation question,
  stop and add it back rather than tracing from code.
- Project knowledge for each session holds ONLY: this kickoff document,
  the variable tracker, the file map, and the ONE module brief currently
  being worked. Nothing else. Swap the module brief out before starting
  the next module's session.
- View the actual current file content fresh before patching - never
  patch against context from earlier in the conversation or an earlier
  session.
- All edits via PowerShell, Apply-Patch (IndexOf/Substring), UTF-8-no-BOM
  I/O. Never Set-Content for writing code.
- Require an explicit "Patched:" / "NOT FOUND:" / "AMBIGUOUS" confirmation
  for every patch. A passing build does not confirm a patch landed. NOTE
  (8b): a NOT FOUND result can also mean a CRLF/LF mismatch in the patch
  script's own string comparison, not necessarily a real content mismatch -
  normalize line endings before comparing, then re-check the file directly
  if a patch unexpectedly fails to match.
- Build after every batch of patches.
- Live-test against real (or deliberately-padded-and-documented) data
  before trusting a fix, not just a clean build.
- Watch for the two recurring bug classes: (a) a standalone function
  outside a hook can't see hook-local variables - pass explicitly as
  parameters; (b) a storage/selector layer that explicitly rebuilds an
  object rather than spreading silently drops unlisted fields (this is
  exactly the bug class Section 5's file-map notes exist to catch -
  see BUSINESS_OUTCOME_FILE_MAP_v6_0.txt Section 4, "the selector
  explicitly lists every field it returns"). Both are now checkable with
  a script before assuming a manual trace is needed - see Section 5:
  check_field_parity.py for (b)-style field drops, check_has_loaded_guard.py
  for the has_loaded race condition.
- NEW (8b): before changing what a shared hook reads or returns, grep for
  EVERY caller of that hook, not just the one you're trying to fix - a hook
  can be called from more than one place (on-page component, an output-
  contract composer, even from INSIDE another core calculation hook). Check
  what each caller actually reads from the return value before assuming a
  change is safe; a narrower dependency than it first appears (e.g. only
  two of a hook's many fields are actually consumed by a given caller) can
  turn an apparent circular-dependency blocker into a safe, scoped change.
- Numbers in living reference docs (file map, variable tracker): NOT
  ALLOWED. These describe mechanism, not current state - they must stay
  true regardless of what data is loaded. Numbers belong only in dated,
  point-in-time records (a module's [VERIFIED 2026-xx-xx] brief entry),
  clearly tied to what they proved, so they read as historical evidence
  rather than a claim about today's data.
- Confirm git status before any commit.

===============================================================================
5. TOOLING AVAILABLE (run locally, paste summary back - saves tokens)
===============================================================================

Location: D:\QSTools\qstools-web\Py\audit-scripts\
  - find_inline_css.py           - inline style={{}} / style="" sweep
  - find_duplicate_css.py        - duplicate CSS rule-block finder
  - find_field_references.py     - find every file/line referencing a
                                    given field name (use this to check
                                    whether a field survives the hook ->
                                    selector -> component chain before
                                    assuming it's broken)
  - check_field_parity.py        - compares a hook's returned fields
                                    against its selector's returned
                                    fields, flags anything dropped
                                    (bug class (b) below). Usage:
                                    check_field_parity.py <hook_file>
                                    <selector_file>
  - extract_calc_map.py (NEW 2026-09-24, v2) - read-only. Extracts every
                                    calculation statement in app/components/
                                    hooks/lib by module (chain order), with
                                    callers, a field index, and a coverage
                                    report of anything it could not parse.
                                    Output: docs\Audit\v6_0\calc_map\
                                    (cleared each run - never save other
                                    files there). Run before changing a
                                    calculation you have not traced, and
                                    paste back 00_COVERAGE.txt first.
  - check_has_loaded_guard.py    - scans the repo for hooks that write
                                    to localStorage inside a useEffect
                                    and flags any missing a has_loaded
                                    guard (Section 4 rule on race
                                    conditions)

Run from repo root (D:\QSTools\qstools-web), e.g.:
    python Py\audit-scripts\find_field_references.py group_overhead_share

Paste the terminal summary back, not the full .txt report, unless more
detail is specifically needed.

WHEN TO RUN EACH (do not run any of these "just in case" - only when the
trigger below actually applies, to avoid wasted token spend reviewing
irrelevant output):

  - find_field_references.py - run when verifying a specific module in
    Phase 2, to confirm a field genuinely exists (or doesn't) across the
    hook/selector/component chain BEFORE spending tokens tracing it by
    reading files manually. Also useful (8b) for finding every CALLER of a
    hook, not just every reader of a field - a simple grep across the repo
    (not limited to this script) served the same purpose in 8b when the
    question was "who else calls this hook", not "who reads this field".

  - check_field_parity.py - run once per hook/selector pair, at the
    point that pair's module is actually reached in Phase 2. Not useful
    before then - there's nothing yet to compare.

  - check_has_loaded_guard.py - optional, repo-wide, no specific module
    dependency. Can be run once early (e.g. during Phase 0/1 setup) to
    seed the variable tracker's Section 8 with a baseline gap list, OR
    deferred and run once during/after Phase 2 Step 4 (Assets - the
    module most likely to touch localStorage-heavy hooks). Either
    timing is fine; running it twice with no code changes in between is
    the only thing to avoid.

  - find_inline_css.py / find_duplicate_css.py - DO NOT run during
    Phase 1/2. These belong to the separate CSS Consolidation brief
    (Section 7), which starts only after v6.0 verification is complete.
    Running them now produces a report that will be stale by the time
    that brief actually starts.

===============================================================================
6. CONSOLIDATION - v5.x BRIEFS FOLDING INTO v6.0
===============================================================================

As each module is reached in Phase 2, its scattered v5.x documents get
folded into ONE current brief for that module before verification starts,
old documents then archived out of project knowledge (not deleted from
the repo - kept for history, just not part of the active working set).

Business Outcome alone currently has ~8-9 separate v5.x documents
(S23, S24, S26, S27, S28, the file map, the CFO gap doc, the session
brief). These consolidate into one Business Outcome v6.0 brief when that
module is reached in Phase 2. DONE 2026-09-23 (core pass) - both the core
pass and the same-day 8b follow-up are recorded in that one brief.

Do NOT consolidate ahead of time for modules not yet being worked -
do it just-in-time, as part of that module's own session, since the
verification work itself will reveal what's still true and what's stale
far better than consolidating blind.

===============================================================================
7. DEFERRED / OUT OF SCOPE FOR THIS PASS (logged so they aren't lost)
===============================================================================

- Import consolidation (moving all imports into one section of the tool,
  ahead of the future Xero API build). Real and worth doing, but a
  separate scoping brief, picked up AFTER the file map and variable
  tracker exist and the v6.0 chain is verified - not before, since the
  map will make it far easier to scope safely.
- CSS consolidation (de-duplicating and tidying stylesheets for easier
  future visual updates). Same treatment - own scoping brief, after v6.0
  verification, informed by the file map.
- Calculation Trace pilot page (lib/calculation-trace/, app page already
  exists, status: code verification deferred). NOT built out further
  during this pass. Once a module's numbers are proven via the plain-text
  variable tracker, THEN that module's real nodes/edges get added to this
  page - populating proven data, not discovering it live.
- Labour/product-based (business_type) coverage gap - tracked as a running note per module
  (Section 1), not fixed inline unless trivial.
- Owner remuneration setting (NEW 2026-09-23, 8b) - a payroll / shareholder
  salary / drawings setting for reconciliation, surfaced during the Business
  Outcome 8b session's cost-gap reconciliation. Belongs to a future Labour +
  Reconciliation session, not this pass's remaining modules. See Section 1.
- BO-12 (NEW 2026-09-23, 8b) - View B's breakdown list needs its own
  unexplained-revenue row to match its headline total when surplus is
  non-zero. Small, self-contained, logged in the Business Outcome brief s6,
  can be picked up whenever - does not block step 9.
- BO-8, BO-10 (carried from the core pass, still open, not scheduled) -
  Business Modelling rate overrides don't reach the capacity coverage gap
  valuation; a cosmetic "TEST" group artefact to remove when convenient.
- Units mode (NEW 2026-09-25) - a full future-build scoping brief exists:
  docs\fin_mirra_v6.0 Briefs\UNITS_MODE_FUTURE_BUILD_BRIEF_2026-09-25.txt.
  Confirmed queued to start after step 9 and CM-1, per this section's
  standing rule. Read that brief in full when it's picked up - do not
  re-derive the design discussion that produced it.

===============================================================================
8. NEXT STEP
===============================================================================

Phase 1 and Phase 2 steps 1-8, including the 8b follow-up, are all done (see
s2). Next: step 9, Business Modelling.

Everything handed to Business Outcome (H1-H7, S-G1, S-H2) is closed. Every
planned 8b follow-up item (F1-F7) plus the one added mid-session (F8) is
closed. What's left from Business Outcome is logged, not blocking: BO-8
(latent), BO-10 (cosmetic), BO-12 (new, small, self-contained), and the new
owner-remuneration design item (belongs to a future Labour/Reconciliation
session, not Business Modelling).

UPDATE (2026-09-25, session 3) - READ THIS FIRST; supersedes every CM-1
  item below. CM-1 FIXED and CM-1b (calculator recovery hours read from a
  missing line type field) FIXED, verified live, committed with the calc
  map (s12) and Business Modelling brief s13. Next session = Business
  Modelling step 5 (brief s13 NEXT). First job: re-run the Business
  Modelling identity checks - its source rows read per_source, whose asset
  rows changed with CM-1. Load the calc map (calculation session).

NEXT SESSION (set 2026-09-24, step 9 session 2):
  UPDATE (late 2026-09-24) - supersedes item 1 below and adds CM-1 inputs:
  - Item 1 DONE: steps 3-4 committed (89bdd1b), build OK; tree clean.
  - SIGNED-OFF RULE for CM-1 (owner, recorded in the Rate Builder units
    scoping brief s2): in an asset-driven unit the calculator rate is the
    combined labour-plus-asset rate; the asset's own charge is the
    DIFFERENCE between the combined rate and the labour rate. Labour is
    not added on top.
    CLARIFIED (owner, late 2026-09-24): 'the labour rate' = the operator's
    saved CHARGE-OUT rate from Rate Builder (what they'd bill alone), NOT
    their cost or minimum rate - cost and overhead already sit in each
    row's true_cost, so subtracting cost would count it twice. Asset row
    revenue = (combined rate - labour charge-out rate) x hours; labour row
    keeps charge-out rate x its hours. Rate Builder's result panel
    (combined - minimum rate) is a MARGIN CHECK, not a revenue split -
    leave it as is. Still for the session to settle in code: combined
    rate below the labour rate (negative asset share), unattended units
    (BO-6), labour-driven units that also have a calculator.
  - PRE-FIX BASELINE committed (b4f33be):
    docs\Audit\v6_0\bo_per_source_PRE_CM1_2026-09-24.json, plus Rate
    Builder calculators, Recovery Outcome and Business Outcome page text
    in the same folder. Compare the post-fix snapshot against it.
  - CAPTURE snapshots by browser DOWNLOAD, never clipboard (clipboard
    saved the PowerShell command instead of the JSON twice on 2026-09-24).
  - QUEUE after step 9: RateBuilder_PathB_and_UnitsMode_v6_0_SCOPING_BRIEF
    (cost-only groups + units mode) - its s8 addendum builds on CM-1.
  UPDATE (2026-09-25) - PIVOTS ITEM 2 BELOW, READ THIS FIRST: the CM-1
    subtraction (calculator rate minus labour charge-out rate) will be
    calculated INSIDE RATE BUILDER, not inside Business Outcome as item 2
    below still literally says. Rate Builder already holds both numbers on
    separate tabs of the same module (the calculator's total charge, and
    the saved per-staff-type charge-out rates in
    qstools_rate_builder_labour_source_rates_v1) - no new cross-module
    plumbing needed. Rate Builder publishes the resulting net asset rate as
    a new field (exact name/shape: decide while reading the real calculator
    selector code fresh next session, do not assume one from this note).
    Business Outcome's build_asset_sources then READS that field directly
    instead of computing the subtraction itself, and no longer needs a
    labour_sources parameter passed into it at all. calculate_capacity_
    coverage_gap gets the same treatment - its asset-driven positive-gap
    value becomes gap_hours x blended_rate only, with nothing added for
    labour on top. The three code-design points item 2 flags as "still to
    settle" are ALL answered, unchanged by the pivot: do NOT clamp a
    negative net asset rate (report it honestly, it is a genuine
    underpricing signal); do NOT add a warning/flag field for it inside
    Business Outcome (that judgment belongs to Business Modelling's floor/
    start/ceiling display, not to Business Outcome's retrospective
    reporting); BO-6's covered-hours mechanism needs no change at all. A
    labour-driven unit that also carries a calculator rate is a data-entry
    mistake, not a case to special-case in code. Full Business Modelling
    Q&A from this same session (net profit display, overhead levers,
    legacy card deletion, materials floor, whole-business ceiling,
    shortfall distribution, lever granularity, reverse-lever direction,
    multi-component calculators) is recorded in
    BusinessModelling_v6_0_BRIEF_CONSOLIDATED_2026-09-24.txt s12 - read
    that section too before resuming step 3 below. A units-mode scoping
    discussion also happened this session and produced its own standalone
    brief - see s7 above, not repeated here.
  1. git log --oneline -8 and git status: confirm the step 3/4 Business
     Modelling commit landed (brief s11 PENDING).
  2. SUPERSEDED 2026-09-25 (see the update above) - kept for history. Fix CM-1 in Business Outcome FIRST (calc map s12). Settle with the
     owner before patching: in an asset-driven unit, the calculator rate is
     the unit's whole revenue; labour's share comes out of it (Rate Builder
     result panel formula) rather than being added. Touches build_labour_
     sources, build_asset_sources, calculate_capacity_coverage_gap and the
     combined totals in useBusinessOutcomePerSourceRevenue.js - a core file,
     so read fresh, confirm, patch, build, live-test, then follow the s4
     CALCULATION CHANGE CHECKLIST (re-run the extract, update the calc map
     lines and close CM-1 there), and commit code + calc map + BO brief +
     file map together.
  3. Then resume Business Modelling at step 5 (brief s11): asset-driven
     units become ONE lever (the all-in unit rate) once CM-1 is fixed.
  Upload for that session: this document, the variable tracker, the file
  map, CALCULATION_MAP_v6_0.txt, and the Business Modelling brief (updated
  2026-09-24, now with a 2026-09-25 s12 addendum recording the
  full Q&A close-out - read that section too). The Business Outcome brief is NOT uploaded - the calc map and
  file map carry the mechanism; record the CM-1 fix as a new dated entry in
  the Business Outcome brief in the repo.

UPDATE 2026-09-24: the consolidation described below is DONE. For the step-9 session
upload only: this document, the variable tracker, the file map, and
BusinessModelling_v6_0_BRIEF_CONSOLIDATED_2026-09-24.txt. Do NOT re-consolidate; the
v5.x Business Modelling docs are superseded and archived out of project knowledge.
(The original wording follows, kept for history.)

Upload for the next session only: this document, the variable tracker, the
file map (BUSINESS_OUTCOME_FILE_MAP_v6_0.txt - still relevant, since Business
Modelling reads per_source directly from Business Outcome's own hooks/
selectors, per Section 3 of that file), and whichever existing Business
Modelling brief/notes exist to consolidate (see Section 6 - this module has
not yet had its v5.x documents folded into one v6.0 brief; do that as the
first step of the step-9 session, same as every other module).
