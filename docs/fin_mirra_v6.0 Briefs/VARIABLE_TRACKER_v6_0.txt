MIRRA v6.0 - VARIABLE TRACKER
STATUS: LIVING DOCUMENT - updated as each module is verified
PURPOSE: trace a key figure (labour cost, asset cost, materials revenue,
overhead) through every stage it passes, stage by stage, so a bug can be
traced to the exact hop where it diverges - without re-deriving the whole
chain from scratch each time.

RULE: NO DOLLAR FIGURES IN THIS DOCUMENT. This describes mechanism (which
field, in which file, becomes which field in the next stage) so it stays
true regardless of what data is currently loaded. Verified real numbers
belong only in the dated [VERIFIED] entry of that module's own brief -
link to that entry from here rather than repeating the number.

Format per stage, per variable: see Section 2 for the template. Fill in
only once a stage has actually been verified - an empty row is more
honest than a guessed one.

===============================================================================
1. THE BACKBONE (shared across labour / asset / materials)
===============================================================================

    P&L  -->  [module: Labour / Assets]  -->  COST ALLOCATION
      (true cost + overhead distribution)
        -->  RATE BUILDER (charge-out rate)
        -->  BUSINESS OUTCOME (revenue vs true cost vs net profit)
        -->  BUSINESS MODELLING

Materials follows a shorter path: P&L / Revenue-COGS import --> Cost
Allocation (COGS side) --> Business Outcome. Confirm exact path when that
stage is reached - not yet verified as of this document's creation.

===============================================================================
2. TRACKER TEMPLATE (copy this block per variable, per stage)
===============================================================================

STAGE: [e.g. "Labour module - true cost"]
FIELD NAME: [exact field name, e.g. true_labour_cost]
FILE / FUNCTION: [e.g. hooks/useLabour.js - calculate_true_cost()]
FEEDS INTO: [next stage's field name + file]
HOURS/UNITS AWARE: [Yes - confirmed / No - assumes hours / Not checked]
VERIFIED: [Not yet / See <module brief name>, [VERIFIED YYYY-MM-DD] entry]
NOTES: [mechanism only - e.g. "rebuilt object in selector, not spread -
  check new fields are added to the explicit return list"]

===============================================================================
3. LABOUR - STAGE BY STAGE
===============================================================================

STATUS: P2.3 verified 2026-09-21; numbers live in Labour_v6_0_BRIEF_VERIFIED_2026-09-21.txt, not here.

STAGE: Labour staff row calculation
FIELD NAMES: total_labour_cost_annual, productive_hours, recovery_hours, paid_hours_per_year
FILE / FUNCTION: lib/calculations/labourCalculations.js (calculateLabourOutputs);
  hooks/useLabour.js (active_staff maps recovery_hours to productive_hours_annual)
FEEDS INTO: lib/selectors/labour/labourOutputContractSelectors.js
HOURS/UNITS AWARE: Hours-native. business_type is never read.
VERIFIED: See Labour v6.0 brief, [VERIFIED 2026-09-21], sections B and C.
NOTES: productive_hours = (paid hours less leave, public holiday, sick and bereavement
  hours) x productivity %. recovery_hours equals productive_hours unless
  contributes_to_recovery_hours is false. Leave is paid inside base cost (rate x paid
  hours) but is not productive. Storage holds inputs only, never calculated totals.
  useLabour filters is_active !== false.

STAGE: Labour output contract
FIELD NAMES: total_labour_cost_annual (sum over ALL active staff incl. non-productive),
  total_people_cost_annual (alias), total_productive_output (sum of staff productive_hours
  field), total_productive_labour_hours and total_productive_labour_cost (from
  calculateProductiveStaffTypeRates: active AND productive staff only),
  weighted_all_productive_labour_rate, productive_staff_type_rates, labour_ready (boolean),
  labour_warnings (alias of productive_staff_type_rate_warnings, no reader)
FILE / FUNCTION: lib/selectors/labour/labourOutputContractSelectors.js
FEEDS INTO (by field name, code search 2026-09-21; not read hop by hop):
  - total_labour_cost_annual: Cost Summary, Revenue Reality, Recovery Summary,
    Cost Allocation group recovery rows, reconciliationRules.js
  - total_people_cost_annual: Cost Summary, Business Summary, Recovery Summary, Revenue
    Reality, Revenue Summary, Quote Engine, Cost Allocation input builder and labour
    adapter, Business Outcome hooks
  - total_productive_output: Cost Summary, Business Summary, Recovery Summary, Quote
    Engine, Business Modelling legacy scenario, reconciliationRules.js
  - productive_staff_type_rates: Cost Allocation (assignment cards, card sections),
    Business Outcome labour recovery, Rate Builder source selector
  - total_productive_labour_hours / weighted_all_productive_labour_rate: Rate Builder only
  - total_productive_labour_cost: Business Outcome, Cost Allocation shared helpers, Rate Builder
HOURS/UNITS AWARE: Hours-native; every consumer works in recovery hours.
VERIFIED: Labour panel maths verified live; downstream consumption traced by name only.
NOTES: two hours names exist (total_productive_output vs total_productive_labour_hours).
  FIXED 2026-09-21 (commit f8c337d, Labour brief F12): total_productive_output is now defined as total_productive_labour_hours (productive staff only). Before the fix, total_productive_output sums the un-zeroed productive_hours field of ALL active staff including non-productive, while the panel field uses productive staff only. Old note: (depends on whether non-productive
  staff's productive_hours field is zero).

STAGE: Labour status / trust
FIELD NAMES: labour_ready (only status field emitted). labour_status and
  labour_model_trust_state are NOT produced by Labour.
FEEDS INTO: reconciliationRules.js (labour_ready), model-readiness page, Rate Builder
  source selector (reads labour_status / labour_model_trust_state, defaults to "ready").
NOTES: Cost Summary and Cost Allocation read no Labour readiness or trust field.
  buildLabourStatus (labourStatusSelectors.js) validates only the editor profile.
  LabourStatusStrip.jsx is not imported anywhere.

===============================================================================
4. ASSETS - STAGE BY STAGE
===============================================================================

STATUS: P2.4 verified 2026-09-23 (commit 26f4be7); numbers live in
Assets_v6_0_BRIEF_VERIFIED_2026-09-23.txt, not here.

STAGE: Asset storage / row
FIELD NAMES: asset_type (productive | support), purchase_price, interest_rate,
  finance_start_date, finance_term_years, finance_paid_off,
  scheduled_hours_per_week, downtime_hours_per_week, utilisation_hours_per_week,
  asset_annual_weeks_override, asset_overhead_pool_assignments (6 pool keys),
  include_depreciation_in_cost, estimated_annual_depreciation, is_retired
FILE / FUNCTION: lib/storage/assetStorage.js (qs_tools_asset_state = editor
  draft; qs_tools_saved_assets = saved list read downstream)
FEEDS INTO: hooks/useAssets.js, hooks/assets/assetOutputContract.js
HOURS/UNITS AWARE: Hours-native. business_type is never read.
VERIFIED: See Assets v6.0 brief, [VERIFIED 2026-09-23], sections 1 and 4.
NOTES: sanitizeSavedAssetRecord re-runs calculateAssetOutputs on every read
  (interest refreshed per load) but passes no business weeks, so stored
  utilisation_hours_annual uses 48 weeks or the override. Stored derived fields
  (asset_recovery_cost_annual etc.) can be stale - read the contract, not raw
  saved rows. Save effects guarded by has_hydrated.

STAGE: Per-asset own cost
FIELD NAMES: asset_interest_annual, total_asset_cost_annual
FILE / FUNCTION: lib/calculations/assetCalculations.js (calculateAssetOutputs)
FEEDS INTO: contract total_asset_cost_annual, productive_asset_cost,
  support_asset_cost
NOTES: total_asset_cost_annual = asset_interest_annual only (running cost
  hard-coded 0). Interest = purchase_price x remaining-term fraction x rate,
  dated to today, finance active only. Principal is cash_flow_support only.

STAGE: Utilisation
FIELD NAMES: utilisation_hours_per_week, utilisation_hours_annual,
  annual_weeks_used, annual_weeks_source
FILE / FUNCTION: hooks/assets/assetHookUtils.js get_asset_utilisation_fields
  (also duplicated in assetCalculations.js, assetSelectorUtilisation.js, storage)
NOTES: weeks = asset override -> Opening Hours annual_open_weeks -> 48.
  Productive only; support = 0. Independent of labour hours.

STAGE: Running-cost pool transfer (from General Overheads)
FIELD NAMES: asset_overhead_pools (GO), asset_overhead_pool_assignments,
  allocated_asset_overhead_cost_annual, asset_recovery_cost_annual,
  total_allocated_asset_overhead_cost_annual
FILE / FUNCTION: lib/selectors/general-overheads/generalOverheadPoolSelectors.js
  (pool amounts, from net active_amount); hooks/assets/assetHookUtils.js
  (summary, effective assignments)
FEEDS INTO: Cost Summary adds total_allocated_asset_overhead_cost_annual to
  Assets and subtracts it from General Overheads.
NOTES: over-assigned pools scaled pro-rata in the contract (brief says block);
  hook-level active_assets uses raw amounts. Latent while pools balance.

STAGE: Recovery rate
FIELD NAMES: required_asset_recovery_rate (per asset),
  productive_asset_recovery_rate (blended), productive_asset_recovery_cost_annual
FILE / FUNCTION: assetHookUtils.js; assetOutputContract.js;
  assetStatusSelectors.js (status version aligned 26f4be7)
NOTES: rate = (own cost + pools [+ opted-in depreciation per asset only]) /
  utilisation hours. Depreciation is not in any total.

STAGE: Output contract -> consumers
FIELD NAMES: total_asset_recovery_cost_annual (full burden), total_asset_cost_annual
  (own), productive_asset_cost / support_asset_cost (OWN, exclude pools),
  *_assigned_overhead_cost_annual, productive_asset_recovery_cost_annual,
  asset_pool_guardrail_status (added 26f4be7), assets_ready, active_assets
FILE / FUNCTION: hooks/assets/assetOutputContract.js
FEEDS INTO (by field name, code search 2026-09-23):
  - total_asset_recovery_cost_annual: Cost Summary Assets line
  - productive_asset_cost: Recovery Summary (own; full burden now built from
    pools, A11 fixed 91c6350), Business Outcome (adds assigned overhead -
    correct), Business Summary (asset groups now sum per-row
    asset_recovery_cost_annual, A11 fixed e517b39)
  - active_assets rows: Cost Allocation asset overlay, reconciliationRules,
    Business Summary / Recovery Summary breakdowns
  - asset_pool_guardrail_status: no consumer yet
HOURS/UNITS AWARE: Hours-native in every consumer.
VERIFIED: contract totals recomputed by hand and matched on screen; see brief s4.

===============================================================================
5. MATERIALS - STAGE BY STAGE
===============================================================================

PARTIAL (2026-09-23, Business Outcome pass) -> CLOSED 8b (F6, H5). Business
Outcome reads total COGS and the Rate Builder materials markup (Path C) inside
hooks/useBusinessOutcomePerSourceRevenue.js. View A: materials revenue = real
revenue less labour and asset modelled revenue (residual). View B: materials
is a peer, revenue = COGS x (1 + markup), true cost = COGS + any positive
residual overhead (build_materials_source, businessOutcomeViewBCalculations.js).
Materials true cost equalled the P&L cost-of-sales total exactly (see Business
Outcome brief). Hop from Revenue/COGS and the Path C markup read: TRACED 8b
(F6) - saved by lib/storage/rateBuilderMaterialsMarkupStorage.js, read once in
the hook, overridable by Business Modelling (effective_materials_markup_percent),
priced in build_materials_source as COGS x (1 + markup). See Business Outcome
brief s4 H5 and s5 BO-9 (the ACHIEVED markup shown on the View B materials row
is a separate, post-cascade figure - do not confuse the saved Rate Builder
markup input with the achieved output).

===============================================================================
6. COST ALLOCATION - OVERHEAD DISTRIBUTION (the hinge - see kickoff doc)
===============================================================================

STATUS: P2.5 verified 2026-09-23 (commits 30de7cc, c40eaad); numbers live in
Cost_Allocation_v6_0_BRIEF_VERIFIED_2026-09-23.txt, not here.

STAGE: Inputs into Cost Allocation
FIELD NAMES: productive_labour_type_rows, support_labour_type_rows,
  all_labour_type_rows (Labour); active_assets / asset_recovery_rows (Assets);
  ~40 Recovery Summary fields incl. overhead_absorbed_cost, recovery_hours_used,
  labour_share_percent, recovery_summary_ready
FILE / FUNCTION: hooks/useCostAllocation.js;
  lib/calculations/cost-allocation/costAllocationLabourAdapter.js (keys by the
  REAL staff_type_id); hooks/cost-allocation/useCostAllocationAssetOverlay.js
  (asset cost = Assets contract row asset_recovery_cost_annual, own + pools);
  lib/calculations/cost-allocation/costAllocationInputBuilder.js (Recovery Summary)
FEEDS INTO: costAllocationRuleBuildContext.js (build_allocation_context)
HOURS/UNITS AWARE: Hours-native. business_type / activity_driver_type are
  passthrough only.
VERIFIED: See Cost Allocation brief, [VERIFIED 2026-09-23], sections 1 and 4.
NOTES: Recovery Summary is UPSTREAM of Cost Allocation (v4 brief had it the
  other way). useCostAllocation calls useRecoverySummary() itself as a
  fallback. Asset hours fall back to Recovery Summary recovery_hours_used when
  an asset has zero utilisation (C10).

STAGE: Source pools
FIELD NAMES: productive_labour_pool, non_productive_labour_pool,
  productive_asset_pool, non_productive_asset_pool, overhead_pool and their
  total_available/assigned/remaining/over_allocated_* fields
FILE / FUNCTION: costAllocationLabourPoolBuilders.js,
  costAllocationAssetPoolBuilders.js, costAllocationOverheadPoolBuilder.js
FEEDS INTO: build_operational_group_cost_rows; output contract (since c40eaad)
NOTES: pool objects live NESTED in *_assignment_result on the calculation
  result, never at its top level - read them from there (C19/C9b root cause).
  Labour assignment cost = staff-type total x assignment_percent;
  assigned_staff_count ignored (C13). Productive/non-productive split uses the
  is_productive flag stamped on the assignment at creation (C17). Overhead pool
  = Recovery Summary overhead_absorbed_cost: hours mode total_business_overheads
  (net of asset pool transfer - verified), product mode total_cost_burden
  (would double count - C3b). Over-allocation detected, not capped (C14).
  Orphaned labour assignment -> silent zero cost (C12).

STAGE: Overhead distribution
FIELD NAMES: overhead_group_assignments (automatic), overhead_allocation_method
FILE / FUNCTION: costAllocationOverheadPoolBuilder.js (build_overhead_pool)
NOTES: automatic only; method inferred (labour_cost_weighted if any productive
  labour assigned, else asset_burden_weighted, else equal_split). Groups with
  no productive labour get zero overhead under labour weighting (C5). Stored
  manual overhead assignments are never read. Weights sum to 1.

STAGE: Group cost rows (the hinge output)
FIELD NAMES: operational_group_cost_rows[] - assigned_labour_cost/hours,
  assigned_asset_burden/hours, assigned_overhead_amount,
  assigned_non_productive_labour_cost, assigned_non_productive_asset_cost,
  total_group_cost, group_recovery_hour_source, group_recovery_hours,
  labour/asset/overhead_recovery_rate, group_cost_per_hour,
  labour_coverage_warning, enriched per-group assignment lists
FILE / FUNCTION: costAllocationGroupCostBuilder.js
  (build_operational_group_cost_rows)
FEEDS INTO: Business Outcome (useBusinessOutcomePerSourceRevenue,
  useBusinessOutcomeWaterfall - REMOVED from the page 8b, F4, dead code never
  rendered; useBusinessOutcome legacy, businessOutcomeAssetSplitCalculations),
  Rate Builder line calculator, costAllocationRateBuilderSelectors
HOURS/UNITS AWARE: hours-native; group_cost_per_unit hard-coded 0.
VERIFIED: every group reconciled to the dollar; see brief s4.
NOTES: total_group_cost includes non-productive cost; the three stream rates
  do not (C6). Recovery hours default to asset hours when a productive asset is
  present (MAX of the group's asset hours, not the sum), else labour hours.
  labour_coverage_warning is computed but not surfaced (C22).

STAGE: Group recovery rows and Rate Builder rows
FIELD NAMES: operational_group_recovery_rows[].minimum_recoverable_rate_per_hour;
  rate_builder_labour_recovery_rows[]
FILE / FUNCTION: hooks/cost-allocation/useCostAllocationGroupRecoveryRows.js,
  then OVERWRITTEN from cost rows in useCostAllocation.js (calculated useMemo);
  lib/selectors/cost-allocation/costAllocationRateBuilderSelectors.js
FEEDS INTO: Business Outcome (minimum recoverable rate per group), Rate Builder
  (rateBuilderLabourSourceSelectors), useBusinessOutcomeLabourRecovery
NOTES: recovery logic v4 said to remove is still here and load-bearing (C7).
  working_unit_recovery_rate_per_hour is stale and unread (C1). Per-staff-type
  Rate Builder rows use each group's actual overhead split; the
  "all_productive" row uses Recovery Summary labour_share_percent instead
  (C24 - A11-affected, handed to Rate Builder). CLARIFIED 8b: this
  rate_builder_labour_recovery_rows stream still exists and is still read by
  Rate Builder itself and by useBusinessOutcomePerSourceRevenue.js's internal
  charge-rate/name lookups - what changed 8b is that Business Outcome's OWN
  Labour Recovery card no longer sources its overhead-per-hour figure from
  this stream (see Section 20 below); this stream's own 100%-on-labour basis
  (RB-1) is unchanged and still correct for Rate Builder's own purposes.

STAGE: Output contract -> consumers
FIELD NAMES: operational_group_cost_rows, operational_group_recovery_rows,
  rate_builder_labour_recovery_rows (the real surface); pool objects and totals;
  unassigned_* ; coverage percents; cost_allocation_ready; Recovery Summary
  passthrough fields
FILE / FUNCTION: hooks/cost-allocation/useCostAllocationOutputContract.js
  (explicit list) + overrides in useCostAllocation.js output_contract
NOTES: explicit list, not a spread - a new calculated field does NOT reach the
  contract unless added (C9 was exactly this). productive_asset_cost here means
  own + pools; in the Assets contract it means own only (C2). Top-level
  assignment lists are raw stored records - read the enriched per-group lists.
  cost_allocation_ready gated on Recovery Summary readiness (C16).
  useCostAllocation() is instantiated separately by each consumer (C15).

===============================================================================
7. CROSS-IMPORT ALIGNMENT (P&L / Balance Sheet / Revenue-COGS)
===============================================================================

CHECK EXISTS: Yes - lib/calculations/crossImportAlignmentCheck.js (pure,
read-only), dev page app/dev/cross-import-check (route /dev/cross-import-check).
STATUS: Built and run against loaded data [VERIFIED 2026-09-20, see the
dated Phase 1 note / P&L brief entry].
MECHANISM: Revenue/COGS holds no imported cost-of-sales data. It reads P&L's
output_contract live (total_revenue, total_direct_costs), so P&L and
Revenue/COGS agree by construction. This supersedes the earlier "same Xero
report" assumption. P&L's revenue and cost of sales are each computed twice
inside calculateProfitAndLoss (section-based vs category-based); the check
compares the two and lists offending lines if they diverge.
PERIOD CHECK: compares P&L financial_year (year ending) + period_month against
the Balance Sheet as_at_date, using financial_year_end_month as an input.

===============================================================================
8. GAPS / OPEN QUESTIONS FOUND DURING VERIFICATION
===============================================================================

-- Log anything found here that doesn't fit the template above - e.g. a
   stage that turns out not to exist, a field that's dead, a hours/units
   gap found mid-module. Date each entry. --

[2026-09-20] Phase 1 findings:
- P&L financial_year defaults from today's date (get_default_financial_year in
  profitAndLossDefaults.js) under a hard-coded 31 March year-end, and the P&L
  importer does not set it from the report header. An unset year can silently
  disagree with a correctly dated Balance Sheet (found live: default rolled to
  the next year while all source documents were the earlier year).
- The tool has no user-supplied financial_year_end_month. The default-year
  logic and the alignment check both need one. Year-end must be per-business
  (NZ commonly 31 March, AU commonly 30 June, both allow other dates).
- P&L importer leaves period_month blank; period is not captured on import.
- Xero COG importer already produces its own account_reconciliation
  (P&L COG total vs Xero net total); where its result is stored is unconfirmed.
- Hours/units coverage: not applicable to Phase 1 (money totals only).
- Dev page year-end selector is React state only and resets on reload.

[2026-09-20] P2.1 Intro page (business type) findings:
- The setting is business_type, values labour_based / product_based. The kickoff
  wording "hours vs units" is naming drift from the code. Storage key
  qs_tools_business_setup_v1 (lib/storage/businessSetupStorage.js), hook
  hooks/useBusinessSetup.js. Save effect is guarded (storage_loaded plays the
  has_loaded role). Invalid stored values fall back to the default.
- Default is labour_based in storage, selector, and every downstream fallback
  (Revenue/COGS, Business Summary, Recovery Summary, Cost Allocation input
  builder and card sections). The completeness check Boolean(business_type) can
  never fail, so a new user counts as complete without choosing, and the
  Recovery Summary missing_business_type warning is unreachable in practice.
  Design decision, not patched.
- Name collision: Quick Start has its own unrelated business_type
  (lib/storage/quickStartStorage.js; values labour_rate_reality_check /
  product_time / m2_rate). Exclude Quick Start files from any business_type grep.
- Four mode vocabularies exist: business_type (labour_based/product_based),
  commercial_driver_mode in Revenue/COGS (hours_based/unit_based/
  mixed_unit_based), recovery_mode in Recovery Summary (hours_based/
  product_unit), activity_driver_type (hours/units, set only in
  get_activity_driver_config, businessSummaryCalculations.js). All derive
  one-way from business_type. No competing stored copy found.
- Recovery Summary re-derives business_type with its own fallback chain (input,
  then revenue_cogs, then labour_based). Duplicated logic, not duplicated state.
- Cost Allocation only passes business_type and activity_driver_type through
  (display: driver label). Its real branching is active_recovery_model, which is
  unrelated to this setting. The labour_only vs labour_led default drift is
  harmless (normalise_recovery_model maps both to labour_led).
- Hours/units coverage: Labour, Assets, Cost Allocation, Business Outcome,
  Business Modelling, Rate Builder and quote-engine do not read business_type.
  All work in recovery_hours and are hours-native in either mode. Whether a
  product business needs units-aware behaviour there is an open design
  question, parked per kickoff Section 1.
- Live check: Recovery Summary driver card follows the setting in both modes
  (hours-based -> recovery hours; product -> units and margin). Product mode was
  checked for wiring only: the test dataset has no unit driver data, so no
  product-mode numbers were exercised.
- Business Summary hierarchy builder is mode-aware: it branches on
  activity_driver_type === "units" and shows the product-mode heading in
  product mode (seen live). Its "Current business mirror" card is a separate
  component (BusinessSummaryStatusStrip.jsx) that uses net annual open hours;
  seen live in both modes (2026-09-20): identical in hours and product mode, so
  it is not mode-aware. Open design question whether an open-hours denominator
  is right for a product business. Not patched.
- Cost Allocation driver label (Units sold / Selected recovery hours) is built in
  lib/selectors/cost-allocation/costAllocationCardSections.js but no component or
  hook under a cost-allocation path reads or renders it (grep, 2026-09-20). The
  mode is carried as data only, so there is no visible label to check.
- Storage write path checked live in both modes: the stored business_type value
  follows the intro page setting.
- Reload behaviour (does a switch update pages without a reload) not observed.
- Cosmetic: the product-mode business type label is defined in three places
  with inconsistent capitalisation (businessSetupSelectors.js and
  BusinessSummaryHierarchyBuilder.js use "Unit-based"; recoverySummaryWarningHelpers.js
  uses "unit-based"). Duplicated label logic, not duplicated state. Not patched.
- lib/selectors/quoteEngineSelectors.js mentions hours-based only in a warning
  string; it does not read business_type.
- Cosmetic: Business Summary mirror shows the currency symbol twice on the
  required operating cost per open hour line (prefix plus unit suffix).
- For P2.5: Business Summary shows a "Cost Summary is not trusted" preview-only
  warning on the test data.

===============================================================================
9. BUSINESS TYPE SETTING - CHAIN (intro page to Cost Allocation)
===============================================================================

STATUS: code trace complete 2026-09-20. Live checks done for storage, Recovery Summary and Business Summary (see Section 8).
HOP 1 - Intro page. FIELD: business_type. FILES: hooks/useBusinessSetup.js,
  lib/storage/businessSetupStorage.js. FEEDS: useRevenueCogs (own
  useBusinessSetup() call).
HOP 2 - Revenue/COGS. FIELDS: business_type, is_product_based, is_labour_based,
  commercial_driver_mode (resolved from business_type; the stored mode only
  distinguishes single vs mixed inside product). FILE: hooks/useRevenueCogs.js.
  FEEDS: output_contract, read by useBusinessSummary and useRecoverySummary.
HOP 3 - Business Summary. FIELD: activity_driver_type via
  get_activity_driver_config (lib/calculations/businessSummaryCalculations.js).
  useBusinessSummary reads business_type from the Revenue/COGS output contract.
  FEEDS: spread into the useRecoverySummary input.
HOP 4 - Recovery Summary. FIELDS: business_type (re-derived), recovery_mode,
  activity_driver_type (passthrough).
  FILE: lib/calculations/recovery-summary/calculateRecoverySummary.js.
  FEEDS: Cost Allocation input builder.
HOP 5 - Cost Allocation. FILES: lib/calculations/cost-allocation/
  costAllocationInputBuilder.js, lib/selectors/cost-allocation/
  costAllocationCardSections.js. Passthrough / display only, silent defaults.
HOURS/UNITS AWARE: hops 1-4 mode-aware; hop 5 passthrough; Labour, Assets and
  everything downstream of Cost Allocation are hours-native.
[VERIFIED 2026-09-20] Code trace complete; storage and Recovery Summary live-checked in both modes; Business Summary hierarchy seen in product mode. Product-mode numbers not exercised (test data has no unit driver data). Reload behaviour not observed.

===============================================================================
10. P&L - STAGE BY STAGE (P2.2 verified 2026-09-21; numbers live in the P&L
    brief P&L_v6_0_BRIEF_VERIFIED_2026-09-21.txt, not here)
===============================================================================
STAGE: P&L entry / import
FIELD NAME: pnl_lines[] (pnl_line_id, line_name, amount, section, category,
  direct_cost_category_id, review_subcategory, interest_treatment)
FILE / FUNCTION: lib/p-and-l/profitAndLossExcelImporter.js, app/api/pnl-import/
  route.js, lib/storage/profitAndLossStorage.js (key qs_tools_profit_and_loss_state)
FEEDS INTO: calculateProfitAndLoss (lib/calculations/profitAndLossCalculations.js)
HOURS/UNITS AWARE: Not applicable - money only; business_type is never read.
VERIFIED: See P&L v6.0 brief, [VERIFIED 2026-09-21] entries.
NOTES: amounts may be stored as text (calculation converts). Excel import leaves
  financial_year and period_month blank; default financial_year is date-derived.

STAGE: P&L calculation - two revenue / cost-of-sales pairs
FIELD NAMES: section-based total_trading_income, total_cost_of_sales,
  gross_profit, net_profit; category-based total_revenue, total_direct_costs
  (= total_cogs), direct_cost_category_totals
FEEDS INTO: category pair -> hooks/useRevenueCogs.js (output_contract);
  section pair -> P&L page, Model Readiness audit panel, cross-import check.
NOTES: the two pairs are equal only when every cost-of-sales line has an active
  direct-cost group and category cogs, and every revenue line is in
  trading_income with category revenue. Nothing enforces this at runtime; the
  dev page /dev/cross-import-check tests it.

STAGE: Labour benchmark
FIELD NAME: labour_benchmark_total (operating-expense lines, category labour)
FEEDS INTO: lib/reconciliation/reconciliationRules.js (wages vs on-cost split
  by line-name pattern) -> Module Reconciliation labour comparison.
HOURS/UNITS AWARE: Not applicable.
NOTES: reconciliation compares this with the Labour module output; it never
  feeds Labour calculations. See Section 12's 8b addition (S-L1) for a
  worked-through instance of this comparison on real data.

STAGE: General overheads pool
FIELD NAME: general_overheads_benchmark_total (operating-expense lines,
  category general_overheads)
FEEDS INTO: reconciliationRules (less asset finance interest moved out, capped
  at the Assets module interest) -> Module Reconciliation. Consumption by the
  General Overheads module (hooks/general-overheads/generalOverheadPnlSync.js)
  is NOT TRACED yet - belongs to P2.5.

STAGE: Asset finance interest
FIELD NAMES: excluded_asset_finance_interest_total (alias of
  pnl_interest_marked_asset_finance_total); assets_benchmark_total (category
  assets, usually near zero); interest_treatment on interest lines
FEEDS INTO: reconciliationRules -> Module Reconciliation asset finance line.
NOTES: interest is recognised by "interest" in the line name; the marked totals
  use operating expenses only, pnl_interest_total uses all sections.

STAGE: Business cost benchmark
FIELD NAME: total_business_costs (= labour + assets + general overheads +
  unassigned; excludes lines categorised excluded)
FEEDS INTO: reconciliationRules (P&L side), Business Summary page, Model
  Readiness. unassigned_balance and pnl_ready also feed reconciliationRules.

===============================================================================
11. P&L GAPS / OPEN QUESTIONS (P2.2, 2026-09-21) - full register in the P&L brief
===============================================================================
[2026-09-21] P&L (P2.2) findings, mechanism only:
- Fixed: a fully blank row counted as needing review, so pnl_ready went false
  with nothing for the user to resolve; review_required_lines now skips rows
  that are completely blank (is_empty_placeholder_line).
- Latent, logged: section pair vs category pair can diverge silently; excluded
  operating-expense lines are invisible in the output contract yet drop out of
  total_business_costs; unrecognised section values drop out of section totals.
- Dead / display-only fields: materials_cost, subcontract_cost,
  hired_equipment_cost, employee_overheads_benchmark_total (hard-coded zero).
- Reconciliation gaps: the "Employee overheads detected" check can never fail;
  the on-cost split matches line names with a short pattern that also hits
  unrelated words; the P&L page reconciliation panel is orphaned.
- Not built from the v4.0 brief: status / trust-state objects, period start and
  end fields, structured warnings, saved account mappings, imported net profit
  variance, other_expenses section.
- Period: no year-end month setting exists; default financial_year is date
  derived and the Reset button restores it; the Excel importer does not read the
  report period.
- Hours/units coverage: none needed for P&L (money only). business_type is not
  read by P&L.
- Accountant-journal lines (e.g. opening work in progress) are treated as
  ordinary direct costs with no review flag; downstream treatment is an open
  question for Revenue/COGS and Business Outcome.
- Classification rules themselves (three separate places) not yet read.
- The Business Outcome file map (BUSINESS_OUTCOME_FILE_MAP_v6_0.txt) was not
  changed by this pass.

===============================================================================
12. LABOUR GAPS / OPEN QUESTIONS (P2.3, 2026-09-21) - full register F1-F15 in the Labour brief
===============================================================================
[2026-09-21] P2.3 Labour findings, mechanism only:
- Trust chain broken at the Labour hop: the brief's six-value labour_status and
  labour_model_trust_state are not emitted; Rate Builder silently defaults the summary
  row to "ready"; Cost Summary and Cost Allocation inherit nothing. Design decision for P2.5.
- labour_ready needs employer contributions and ACC levy both above zero, which is stricter
  than the brief and omits its staff-type and zero-hour checks.
- Reconciliation on-cost split matches line names by substring ("acc"); latent on current
  data. Better key is review_subcategory. reconciliationRules.js is 1,035 lines, so put the
  classifier in a new file.
- FIXED 2026-09-21 (commit f8c337d): total_productive_output is now the productive-staff figure. Was: total_productive_output includes non-productive staff's un-zeroed productive_hours, so it exceeds total_productive_labour_hours. The two names (total_productive_output, total_productive_labour_hours) feed different
  consumers. Downstream effect depends on which field each reads (see Labour brief F12).
- Readiness UI validates the open editor profile, not the saved staff; dead
  LabourStatusStrip and a stale comment naming it as a consumer.
- is_productive is derived from labour_class; no staff include/exclude toggle in the UI.
- Reconciliation "timing lag" wording is shown regardless of cause; Owner/Director card
  compares amounts only.
- Business Summary required_labour_burden_rate reads a field its calculation never produces, so it shows zero (F16).
- Legacy Business Modelling baseline snapshots keep old hours until re-locked (F17).
- FIXED 2026-09-23 (F18, commit 51e2e08): business_recovery_hours was one productive person's average hours (productive output / productive staff count). Decided: pricing views use total productive person-hours; macro views use the opening calendar. getBusinessRecoveryHours now returns total_productive_output. See Summaries v6.0 brief S5 and tracker Section 16.
- FIXED 2026-09-22 (commit 25644b8): statutory NZ rest breaks were not deducted from
  productive hours (F19, full detail in Labour brief). New jurisdiction-pluggable break-
  rule module; labourCalculations.js now derives worked days from leave, then folds paid
  rest-break minutes into non_productive_paid_hours. Downstream effect confirmed live:
  required_recovery_rate, people_cost_per_recovery_hour and business_recovery_hours all
  moved accordingly; weighted_productivity_percent unchanged (consistency guard held).
  Jurisdiction is hardcoded NZ for now - no Business Setup field yet (forward item, see
  kickoff). Asset-level break impact (operator-bound vs staffed/staggered assets) deferred
  to the Assets pass (P2.4).
- Hours/units coverage: Labour is hours-native and does not read business_type.

[2026-09-23] Added during Business Outcome 8b follow-up session (not a Labour
  session in its own right - logged here as it belongs to this module, but not
  yet independently verified against Labour's own code):
- S-L1 (Summaries brief, tracker Section 17) - labour variance vs P&L
  benchmark - substantially explained: a P&L operating-expense reconciliation
  during the 8b cost-gap investigation traced most of the labour benchmark
  variance to Owner/Director's own labour cost. Mechanism, not fixed: no code
  changed - this was a diagnostic exercise, not a Labour-module patch.
- NEW DESIGN ITEM, logged for a future Labour + Reconciliation session (not
  scoped or built): an owner-remuneration setting is proposed
  (payroll / shareholder salary / drawings), to be read by
  reconciliationRules.js, distinguishing which basis the P&L's owner-related
  lines are actually on. See Business Outcome brief s5 ("Owner remuneration -
  design item") for the full note and real numbers from the 8b session.
  This item belongs to Labour/Reconciliation, not Business Outcome - carried
  here as the authoritative place to pick it up, cross-referenced from the
  Business Outcome brief.

===============================================================================
13. ASSETS GAPS / OPEN QUESTIONS (P2.4, 2026-09-23) - full register A1-A23 in the Assets brief
===============================================================================
[2026-09-23] P2.4 Assets findings, mechanism only:
- FIXED 26f4be7: A10 Assets page blended rate used own cost only (now own + pools);
  A18 pool guardrail status now in the output contract; A21 zero-cost productive
  asset warning; A23 AssetStatusStrip was never rendered, so no Assets warning was
  ever visible (now rendered).
- NAMING TRAP: productive_asset_cost / support_asset_cost are own cost and
  exclude assigned pools. Do not widen them at source - Business Outcome adds the
  overhead itself and would double count.
- FOR P2.5 (A11): Recovery Summary rate / asset_recovery_cost / split basis and
  the Business Summary asset breakdowns treat own cost as the full burden. Fix spec
  in the Assets brief s5. Recovery Summary half FIXED 2026-09-23 (see Section 15);
  Business Summary half FIXED 2026-09-23 (e517b39, Summaries brief S1).
- FOR P2.5 (A12/A13): General Overheads publishes clashing asset_* field names
  and reads saved assets through its own storage instance.
- DESIGN: A1 own cost is interest only (no depreciation / lease / ownership
  inputs); A2 opt-in depreciation reaches the per-asset rate only; A4 interest is
  a today-dated straight-line estimate, not financial-year aligned; A6 brief
  contradicts itself on partly assigned pools.
- LATENT: A3 three different over-assignment treatments; A5/A20 duplicated
  weeks logic and stale stored derived fields.
- Business Outcome file map not changed by this pass.
- Tooling: check_has_loaded_guard.py baseline run 2026-09-23 (first run). Flags
  assetStorage.js falsely (guard named has_hydrated); profitAndLossStorage.js,
  labourRateRealityCheckStorage.js and two Quick Start storage files also
  flagged - P&L one unconfirmed, others outside the chain. check_field_parity.py
  flattens row-level fields into top-level results.
- Hours/units coverage: Assets is hours-native and does not read business_type.
- Test data: CONFIRMED 2026-09-23 (Business Outcome pass) - synthetic assets are
  PC15 and 2inc Line Pump 2. Mini Truck is real; reclassified to support.

===============================================================================
14. COST ALLOCATION GAPS / OPEN QUESTIONS (P2.5, 2026-09-23) - full register C1-C24 in the Cost Allocation brief
===============================================================================
[2026-09-23] P2.5 Cost Allocation findings, mechanism only:
- VERIFIED: every source dollar is distributed exactly once. Labour, productive
  and non-productive assets and overhead reconcile to grouped operating cost,
  Cost Summary total and Recovery Summary cost burden; nothing unassigned.
- FIXED 30de7cc (C19): asset and overhead pool panels showed zero assigned -
  cards read pool fields at the top level of the calculation result, where they
  never exist. Cards now lift the nested *_assignment_result objects.
- FIXED c40eaad (C9/C9b): contract pool objects, overhead total and method, and
  the S18 non-productive fields were undefined / zero / empty. No consumer read
  them; now correct for future readers.
- CLOSED: overhead pool is net of the asset pool transfer in hours mode (no
  double count). A11 does not reach any Cost Allocation figure that is read.
- DESIGN: product mode would double count via the overhead pool (C3b);
  overhead method inferred, asset-only groups get none (C5); minimum recoverable
  rate excludes non-productive cost vs Business Outcome charge_to_self (C6);
  recovery logic still lives here (C7); status gated on Recovery Summary and a
  single Assets review warning blocks the whole chain (C16); support-vehicle
  running costs cannot follow the vehicle until Assets A1 is decided (C18).
- LATENT: orphaned assignments compute silently to zero (C12, has happened
  once); assigned_staff_count ignored, card uses a different formula (C13);
  over-allocation not capped (C14); productive flag frozen at assignment time
  (C17); labour coverage gap computed but never shown (C22, live on this data);
  zero-hour asset falls back to whole-business hours (C10).
- DISPLAY: non-productive partly hidden in pool panel (C20); status strip
  possibly not rendered (C21, unconfirmed); stream hour labels show group hours
  (C23); four warning flags hard-coded false (C8).
- HANDED ON: C24 to Rate Builder (all_productive row uses Recovery Summary
  labour share; Assets A11 is a prerequisite). C6 to Business Outcome.
- Breakeven residuals move daily because asset interest is dated to today
  (Assets A4). Treat a residual explainable by interest decay as a pass -
  CONFIRMED again during the 8b Business Outcome session (~$26/day drift
  observed live, same-day re-reads, consistent with this note).
- Tooling: check_has_loaded_guard false positive again (guard named hydrated);
  check_field_parity compared calculation_inputs, not the contract.
- Hours/units: passthrough only; group_cost_per_unit hard-coded 0.
- Business Outcome file map not changed by this pass.
- Test data: strip-or-carry DECIDED at this close-out - carry (commit 1e5009f).

===============================================================================
15. RECOVERY SUMMARY - A11 FIX (2026-09-23) - record in A11_RecoverySummary_v6_0_BRIEF_VERIFIED_2026-09-23.txt
===============================================================================
STAGE: Recovery Summary asset stream
FIELD NAMES: productive_asset_cost (own, unchanged), productive_asset_assigned_
  overhead_cost (new), productive_asset_recovery_cost_annual (new, own + pools),
  asset_recovery_cost, required_asset_recovery_rate, asset_recovery_status,
  labour/asset/overhead_share_percent
FILE / FUNCTION: lib/calculations/recovery-summary/calculateRecoverySummary.js
FEEDS INTO: useRecoverySummary output_contract -> Cost Allocation input builder
  (labour_share_percent -> Rate Builder all_productive row, C24; asset_recovery_
  cost -> working_unit_recovery_cost, dead C1), legacy useBusinessOutcome
  (asset stream recoveryTarget).
HOURS/UNITS AWARE: hours mode only; product mode zeroes every stream except
  overhead.
VERIFIED: See A11 record, [VERIFIED 2026-09-23], s5.
NOTES: the calculation input is Business Summary outputs spread at top level
  plus the Assets contract nested as input.assets. Business Summary publishes
  productive_asset_cost (own) but not the assigned-pool figure - read pools
  from input.assets. Published shares derive from stream costs, never from the
  saved split. The raw calculated object never leaves the hook (status, card,
  output_contract only).
GAPS (mechanism only):
- R1 FIXED: asset_recovery_status computed but not returned (card showed
  "Not selected", contract published undefined).
- R2: saved split controls never reach any output (streams overwritten in both
  modes). Design decision - logged, not patched.
- R3: support asset own cost (and support pools) sit in no hours-mode stream.
  RECLASSIFIED BY DESIGN 2026-09-23: Cost Allocation takes overhead_absorbed_cost
  as its overhead pool and distributes support assets through its own
  non-productive asset pool, so adding them to the overhead stream would double
  count. Only the stream shares are affected; handed to Rate Builder with C24.
- R4: RecoverySummaryStatusStrip.jsx duplicates main-card/
  RecoverySupportingEvidenceDetail.jsx. Cleanup - logged.
- R5 = Labour F18. FIXED 51e2e08 (Section 16).
- Business Summary half of A11 FIXED e517b39.
- Business Outcome file map not changed by this fix.

===============================================================================
16. OPENING HOURS / COST SUMMARY / BUSINESS SUMMARY (P2.6, 2026-09-23) - numbers in Summaries_v6_0_BRIEF_VERIFIED_2026-09-23.txt
===============================================================================
STATUS: P2.6 verified 2026-09-23 (commits 47ca86d, e517b39, 13f3c58, 51e2e08).

TWO HOUR BASES (decided 2026-09-23) - keep apart:
  MACRO (Cost Summary, Business Summary display): per open hour / open day /
    open week, from Opening Hours only.
  PRICING (Recovery Summary and everything downstream): total productive
    person-hours (business_recovery_hours).

STAGE: Opening calendar
FIELD NAMES: net_annual_business_open_hours, net_annual_business_open_days
  (new), annual_open_weeks, standard_daily_open_hours
FILE / FUNCTION: lib/calculations/openingHoursCalculations.js
FEEDS INTO: Cost Summary via useModelReadiness().modules.openingHours
  (read as .calculated first - the raw calculation return); Assets reads
  annual_open_weeks the same way. As of 8b (F1, Business Outcome), also
  threaded through to Business Outcome's own display scaling - see Section 20.

STAGE: Cost Summary calculation
FIELD NAMES: total_cost_burden (labour total + asset recovery cost incl pools
  + general overheads after pool transfer), macro_required_operating_hour_rate
  (= burden / net open hours), net_annual_business_open_days,
  annual_open_weeks, business_recovery_hours, required_recovery_rate
  (= burden / business_recovery_hours), required_labour_burden_rate
FILE / FUNCTION: lib/calculations/costSummaryCalculations.js;
  lib/selectors/costSummarySelectors.js (selectCostSummaryLabour,
  getBusinessRecoveryHours; card selector); hooks/useCostSummary.js
FEEDS INTO: useCostSummary output_contract -> useBusinessSummary; card ->
  app/cost-summary/page.jsx -> CostSummaryCard / CostSummaryDrill.
HOURS/UNITS AWARE: hours only; business_type not read.
NOTES: every hop is an explicit field list (bug class b) - a new field must
  be added at the calculation return, the output contract, the card selector,
  the page and the card. required_recovery_rate is read outside the core
  chain by Quote Engine, recovery analysis, Model Readiness panels and legacy
  Business Modelling. Model readiness enters as useModelReadiness().status.

STAGE: Business Summary calculation
FIELD NAMES: recovery_hours_used, activity_driver_value,
  required_recovery_per_driver, current_margin_per_driver,
  recovery_gap_per_driver, *_per_recovery_hour, actual_recovery_rate,
  profit_or_deficit_per_recovery_hour
FILE / FUNCTION: lib/calculations/businessSummaryCalculations.js;
  hooks/useBusinessSummary.js (calculation input, status input,
  display_details -> card, output_contract - four separate explicit lists)
FEEDS INTO: spread at top level into the Recovery Summary calculation input.
NOTES: these fields are on the PRICING basis (person-hours) and exist for
  Recovery Summary. The Business Summary screen no longer displays them in
  hours mode.

STAGE: Business Summary display
FIELD NAMES: net_annual_business_open_hours, net_annual_business_open_days,
  annual_open_weeks (from the Cost Summary contract)
FILE / FUNCTION: BusinessSummaryCard.jsx (Open-Hour Reality values computed
  in the card), BusinessSummaryHierarchyBuilder.js,
  hierarchy/businessSummaryHierarchyScaleUtils.js (createDisplayScalers),
  BusinessSummaryCardUtils.js (scaleAnnualValue, scalePeriodValue),
  BusinessSummaryMacroPositionCard.jsx (P&L bridge)
NOTES: shared helpers take days / weeks as optional parameters and fall back
  to 260 / 52 - CLOSED 8b (S-H2, Business Outcome F1): Business Outcome now
  calls these with real open_days / open_weeks instead of relying on the
  fallback (see Section 20). Product mode uses scaleProductAnnualValue (/365),
  untouched. P&L bridge: overhead line adds back running costs moved to
  Assets; proof rows signed; "other classification differences" closes the
  proof.

STAGE: Trust / readiness
FIELD NAMES: model_readiness_status -> model_trust_state ->
  recovery_summary_status / recovery_summary_ready -> Cost Allocation status
FILE / FUNCTION: lib/calculations/modelReadinessCalculations.js (any
  reconciliation check with is_blocking -> blocked);
  recoverySummaryStatusSelector.js; costAllocationStatus.js
NOTES: warnings pass; a single blocking reconciliation check sets Cost
  Allocation to not_supported. Working as designed (C16 diagnosed).

===============================================================================
17. SUMMARIES GAPS / OPEN QUESTIONS (P2.6, 2026-09-23) - full register in the Summaries brief s5
===============================================================================
[2026-09-23] P2.6 findings, mechanism only:
- FIXED: Business Summary A11 groups; mixed hour bases on Business Summary;
  per day / per week ignored the opening calendar; P&L bridge did not close;
  F18 at source.
- C16 DIAGNOSED: blocked by the business cost benchmark reconciliation. The
  test data's synthetic staff and assets are not in the P&L, so this is
  expected. No code change; wording question logged.
- R3 RECLASSIFIED by design (Section 15).
- OPEN CHECK (S-G1): whether the General Overheads module includes the P&L
  line marked as asset finance interest while Assets also calculates
  interest (possible double count). CLOSED 2026-09-23 (Business Outcome brief
  BO-7): General Overheads floors the line at zero - no double count. Excess
  traced to synthetic assets' interest plus Assets' today-dated estimate
  overshoot (A4).
- OPEN (S-L1): labour variance vs P&L only partly explained by synthetic
  staff - Labour / P&L classification review. SUBSTANTIALLY EXPLAINED 8b
  (2026-09-23, Business Outcome follow-up session): see Section 12's 8b
  addition and the Business Outcome brief s5 for the owner-remuneration
  design item this uncovered. Not fully closed - a residual remains, logged
  as a Labour/Reconciliation follow-up, not yet independently re-verified
  from Labour's own module.
- OPEN (S-O1): Opening Hours test data has 19-hour and 24-hour days.
- HANDED ON: C24 + R3 share -> Rate Builder; Business Outcome day / week
  wiring - CLOSED 8b (S-H2, F1); Quote Engine now per person-hour, unverified
  (S-H3); legacy Business Modelling baseline needs re-lock (S-H4).
- CLEANUP: R4 duplicate strip; unused *_per_recovery_hour props and
  sumAssetCost; required_labour_burden_rate now redundant.
- Hours/units: macro views hours-mode only; product mode untouched.
- Business Outcome file map not changed by this pass (WAS changed 8b - see
  BUSINESS_OUTCOME_FILE_MAP_v6_0.txt's own 2026-09-23 8b update line).

===============================================================================
18. RATE BUILDER (2026-09-23) - record in RateBuilder_v6_0_BRIEF_VERIFIED_2026-09-23.txt
===============================================================================
STATUS: code-traced 2026-09-23, closed without live numbers (no calculated
  Rate Builder figure propagates). No code changed in the Rate Builder session
  itself. CLARIFIED 8b (Business Outcome follow-up, see Section 6 and 20):
  Rate Builder's own rate_builder_labour_recovery_rows stream and its 100%-
  on-labour overhead basis (RB-1) are UNCHANGED and still correct for Rate
  Builder's own purposes - what changed 8b is only that Business Outcome's
  Labour Recovery card stopped reading this stream for its own overhead-per-
  hour figure, and switched to Business Outcome's own per-group split
  instead. Rate Builder itself was not touched.

STAGE: Labour Rates Builder (Path A)
FIELD NAMES: labour_cost_rate, allocated_business_overhead_recovery_rate,
  minimum_recoverable_charge_out_rate, current_charge_out_rate,
  weighted_current_charge_out_rate
FILE / FUNCTION: hooks/rate-builder/useRateBuilderLabourRates.js;
  lib/selectors/rate-builder/rateBuilderLabourSourceSelectors.js
  (build_labour_source_options, build_recovery_fields);
  lib/selectors/cost-allocation/costAllocationRateBuilderSelectors.js
  (build_rate_builder_labour_recovery_rows)
INPUTS: Labour productive_staff_type_rates[].weighted_productive_hourly_rate,
  weighted_all_productive_labour_rate; Cost Allocation
  rate_builder_labour_recovery_rows.
FEEDS INTO: display only. Saved charge rates (user input) go to storage key
  qstools_rate_builder_labour_source_rates_v1, keyed by staff_type_id, read
  directly by Business Outcome (readRateBuilderLabourSourceRates) - this
  charge-rate read is UNCHANGED by the 8b Labour Recovery fix; only the
  overhead-per-hour figure changed source (see Section 20).
HOURS/UNITS AWARE: hours-native; business_type not read.
NOTES: minimum rate = labour cost rate + that staff type's overhead share /
  its Cost Allocation assigned hours. Per-staff rows put 100% of each
  group's overhead on labour (RB-1, unchanged - see STATUS note above).
  all_productive row uses Recovery Summary labour share (C24). labour status
  defaults to "ready" (RB-Q1). calculate_labour_rate_result forces efficiency
  100 / allowance 0 - its sell-rate outputs are unused.

STAGE: Customer Charge Calculator (Path B)
FIELD NAMES: operational_group_cost_rows[].group_cost_per_hour,
  labour/asset/overhead_recovery_rate, group_recovery_hours;
  recovery_driver_quantity, recovery_preview
FILE / FUNCTION: components/rate-builder/line-calculator/
  useRateBuilderLineCalculator.js; lib/calculations/rateBuilderCalculations.js
FEEDS INTO: display only. Calculators (user input) go to storage key
  qs_tools_rate_builder_calculators_v1, keyed by
  linked_cost_allocation_group_id, read by Business Outcome.
NOTES: only groups with assigned_asset_burden > 0 offered. Recovery hours =
  sum of "hr" line quantities, else output-driver quantity (RB-13). Card
  mixes group rate and Labour-tab minimum rate (RB-12). No linked_asset_id.

STAGE: Output contract
NOTES: none exists. Rate Builder reads no readiness / trust field.

===============================================================================
19. RATE BUILDER GAPS (2026-09-23) - full register RB-1..RB-17 in the brief
===============================================================================
- Parked display defects: RB-1 (still valid within Rate Builder itself - see
  Section 18 STATUS note; only Business Outcome's own consumption of this
  basis changed, 8b), RB-2, RB-3/C24, RB-5, RB-12, RB-13, RB-15, RB-17. Parked
  design / cleanup: RB-7, RB-10, RB-11, RB-14, RB-16, RB-Q2.
- Not built from v4.0: output contract, status / warnings, margin / markup
  and suggested sell rate, rate comparisons, unit and subset rates, Recovery
  Link tab.
- Handed to Business Outcome: storage ids (H1, CLOSED), calculator group links
  and S28 (H2, handled, S28 itself still open design), C6 (H3, see BO-3 design
  decision), C24 if read (H4, CLOSED - never read), materials markup (H5,
  CLOSED 8b - see Section 5), S-G1 (H6, CLOSED - see Section 17), test-data
  decision (H7, CLOSED).
- Hours/units: hours-native; business_type not read.
- Business Outcome file map not changed by this pass.

===============================================================================
20. BUSINESS OUTCOME (2026-09-23 core pass + 2026-09-23 8b follow-up) - record
    in BusinessOutcome_v6_0_BRIEF_VERIFIED_2026-09-23.txt
===============================================================================
STATUS: core chain verified live 2026-09-23 (commits ab7d721, b668bbd). 8b
  follow-up session SAME DAY closed every item in the follow-up brief (F1-F8,
  commits a401c5e, d4aa80c, 4bdaad4). Step 8b is DONE - see kickoff s2 step 8b.

STAGE: Labour sources
FIELD NAMES: labour_sources[] - hours (revenue hours), direct_cost,
  overhead_share, true_cost, charge_out_rate, modelled_revenue,
  minimum_recoverable_rate_per_hour (group rate, BO-3 - labelled "Group min
  rate" on the card as of 8b F3), non_productive_share (NEW 8b, BO-1 Part B -
  each row's own slice of its working unit's non-productive cost, stamped by
  distribute_non_productive_cost; a row's full cost incl. support share =
  true_cost + non_productive_share)
FILE / FUNCTION: hooks/useBusinessOutcomePerSourceRevenue.js build_labour_sources
INPUTS: Cost Allocation operational_group_cost_rows[].labour_group_assignments
  (assigned_cost, assigned_hours), assigned_overhead_amount; Rate Builder saved
  charge rates via useBusinessOutcomeLabourRecovery (keyed staff_type_id).
NOTES: one row per (group, staff type). Group overhead split labour/asset by
  cost share, then by hours share. Revenue hours: labour-driven group = own
  hours; asset-driven = covered hours x hours share.

STAGE: Asset sources
FIELD NAMES: asset_sources[] - blended_rate (ALL-IN), net_asset_rate, labour_mix_charge_rate (NEW 2026-09-25), modelled_revenue, direct_cost,
  overhead_share, true_cost, non_productive_share (NEW 8b, same mechanism as
  labour sources above)
FILE / FUNCTION: build_asset_sources (same hook); get_group_blended_rate
  (calculator keyed linked_cost_allocation_group_id)
NOTES: (2026-09-25, CM-1/CM-1b) blended_rate = total_charge /
  get_calculator_recovery_hours (unit "hr" lines) = the ALL-IN unit rate;
  net_asset_rate = blended_rate - labour_mix_charge_rate (hours-weighted
  saved charge-out rates of the unit's assigned labour); revenue =
  net_asset_rate x covered hours, shared by asset direct-cost share. A
  unit's labour + asset revenue = all-in rate x covered hours.

STAGE: Covered hours (BO-6)
FIELD NAMES: get_group_covered_hours(group);
  group_real_capacity[].group_recovery_hours (now covered hours)
NOTES: asset-driven = min(seat hours, sum of assigned labour hours); zero
  labour = zero revenue. Business Modelling lever calculations read
  group_real_capacity hours, so they follow automatically.

STAGE: Non-productive distribution and cascades
FIELD NAMES: non_productive_cost_distributed_total, pure_support_groups,
  real_capacity.group_real_capacity[] (true_cost, non_productive_cost,
  naive_net_profit, final_net_profit), view_b.real_capacity,
  headline_real_capacity, view_b_headline_real_capacity
FILE / FUNCTION: distribute_non_productive_cost, apply_real_capacity (hook);
  apply_real_capacity_v2 (businessOutcomeViewBCalculations.js); selector builds
  headlines.
FEEDS INTO: Business Modelling (per_source: headline_real_capacity,
  view_b_headline_real_capacity, group_real_capacity).
NOTES: injected true cost lives ONLY in group_real_capacity; source rows keep
  uninjected cost (BO-1) - FIXED 8b: distribute_non_productive_cost now ALSO
  stamps row.non_productive_share directly on each row in both cascades, so
  any display can add it to the row's own true_cost without needing to
  cross-reference group_real_capacity separately (see field-name trap above
  and file map gotcha #4). all_sources[] has no total_cost, so Business
  Modelling cannot pick up the stale figure (unaffected either way).

STAGE: Capacity coverage gap
FIELD NAMES: capacity_coverage_gap[] (gap_type, gap_hours, gap_dollar_value),
  labour_coverage_gaps
NOTES: revenue_at_risk now means revenue NOT earned (excluded by BO-6), valued
  at asset blended rate + assigned labour weighted charge rate.

STAGE: View B headline - unexplained revenue (NEW 8b, F8/BO-11)
FIELD NAMES: view_b_headline_real_capacity.unexplained_revenue; the same
  mechanism appears inline in the card's own build_view_b_headline
FILE / FUNCTION: apply_real_capacity_v2 already returned a `surplus` value
  (businessOutcomeViewBCalculations.js) - previously computed but never
  consumed. Now added into total_net_profit and published as
  unexplained_revenue by both the selector and the card.
NOTES: surplus = real revenue above what View B's modelled sources explain;
  0 whenever real revenue does not exceed modelled. Not yet broken out as its
  own row in the visible per-source breakdown list (BO-12, logged, not built -
  the breakdown list can currently sum to less than the stated headline when
  surplus is non-zero).

STAGE: Labour Recovery card - overhead-per-hour (REWORKED 8b, F7/BO-2)
FIELD NAMES: overhead_per_hour, true_cost_per_hour, allocated_business_
  overhead_recovery_rate (internal to build_overhead_rate_map)
FILE / FUNCTION: hooks/useBusinessOutcomeLabourRecovery.js
  build_overhead_rate_map(labour_groups) - now takes per_source.labour_groups
  (default []) instead of Rate Builder's rate_builder_labour_recovery_rows.
  Aggregates overhead_share and hours per staff type across every group that
  type appears in (hours-weighted average), using the SAME per-group cost-
  share split (split_group_overhead) as every other figure on this page,
  rather than Rate Builder's separate 100%-on-labour basis (RB-1).
FEEDS INTO: BusinessOutcomeTruthLabourRecoveryCard.jsx (unchanged component -
  only its data source changed); useBusinessOutcomeOutputContract.js (also
  updated 8b to compute its own per_source and pass labour_groups through, so
  AI Export / Dashboard / Business Modelling see this same basis, not a stale
  Rate-Builder-based copy).
NOTES: for a staff type working only in asset-less groups, the two bases
  (Business Outcome's cost-share split vs Rate Builder's 100%-on-labour) are
  mathematically identical - asset_cost = 0 collapses split_group_overhead to
  the same 100%-on-labour result - so those rows are provably unchanged by
  this fix. Only staff working a productive-asset group can shift. The
  internal call to this hook from inside useBusinessOutcomePerSourceRevenue.js
  itself (no arguments passed) is unaffected: it only reads charge_out_rate
  and labour_source_type_name off the returned rows, never the overhead
  fields - traced and confirmed 8b, no circular dependency.

STAGE: Readiness
NOTES: per-source hook reads no readiness field; useBusinessOutcomeTruth gates on
  revenue_cogs_ready, cost_summary_ready, business_summary_ready only.
HOURS/UNITS AWARE: hours-native throughout; business_type not read.

===============================================================================
21. BUSINESS OUTCOME GAPS (2026-09-23 core + 8b) - full register BO-1..BO-12 in the brief
===============================================================================
- FIXED (core pass): BO-6 covered-hours cap (calculation + wording).
- FIXED (8b): BO-1 stale cost columns / drill children (F2, Part B);
  BO-2 Labour Recovery card overhead basis (F7 - option (a) chosen: card now
  uses Business Outcome's own cost-share split); BO-3 label "Group minimum
  rate" (F3); BO-4 dead waterfall computation removed (F4); BO-9 View B
  minimum markup resolved and relabelled "Achieved markup"; BO-11 View B
  headline now counts unexplained revenue (F8 option a).
- OPEN, logged, not scheduled: BO-8 gap valuation ignores Business Modelling
  overrides (latent). BO-10 test artefact group "TEST" - cosmetic, remove
  when convenient. BO-12 (NEW 8b) - View B's breakdown list doesn't yet carry
  unexplained_revenue as its own row, so it can undercount vs the headline
  when surplus is non-zero; small self-contained pass, not blocking step 9.
- OPEN design (unchanged from core pass): unattended / dry-hire exemption;
  concurrent attendance; S28 per-asset linking; product mode (business_type
  gap, kickoff s1).
- NEW design item (8b, logged for Labour/Reconciliation, not Business
  Outcome): owner remuneration setting - see Section 12's 8b addition.
- CLOSED: H1, H2 (handled), H4, H5 (CLOSED 8b, F6 - materials markup path
  traced, also resolves BO-9), H6/S-G1 (BO-7 explains the interest excess),
  H7 (carry, 1e5009f, breakeven dropped 8b per F5), S-H2 (CLOSED 8b, F1).
- Test data: PC15 and 2inc Line Pump 2 synthetic; Accountant assumed synthetic;
  Mini Truck reclassified to support. Breakeven revenue dropped 8b (F5) - real
  revenue restored and re-verified, see brief s7.
- Business Outcome file map updated by this pass (core: gotcha #5, BO-6; 8b:
  three path/location corrections, gotcha #6, Section 3/5 updates for F7/BO-1/
  BO-11 - see BUSINESS_OUTCOME_FILE_MAP_v6_0.txt's own dated update lines).
- SESSION CLOSE (8b): all follow-up items (F1-F8) resolved same day as the
  core pass. Nothing carried forward blocks step 9 (Business Modelling).

===============================================================================
22. BUSINESS MODELLING - STAGE BY STAGE
===============================================================================

STATUS: step 5 UI cleanup [VERIFIED 2026-09-25]; numbers live in
  BusinessModelling_v6_0_BRIEF_CONSOLIDATED_2026-09-24.txt, not here. Asset-driven
  units as ONE lever (labour rows cost-only) NOT yet built - see that brief's
  STILL OPEN note. OPEN: a small unexplained drift exists between the
  2026-09-24 and 2026-09-25 dated real-net-profit figures in that brief -
  cause not yet identified (data edit vs calc-path change not yet
  distinguished); verify before trusting either dated figure as current.

STAGE: Source rows (per lever-able source)
FIELD NAMES: buildSourceRows() output - basis, achieved_revenue, start_rate,
  floor_rate, ceiling_rate, group_min_rate, status, leverable
FILE / FUNCTION: lib/calculations/businessModellingSourceRows.js (buildSourceRows)
FEEDS INTO: lib/calculations/businessModellingAdditiveModel.js (applyAdditiveLevers)
HOURS/UNITS AWARE: hours-native (covered hours as basis); business_type not
  read by any Business Modelling file (matches Section 1's kickoff-level note).
VERIFIED: See Business Modelling brief, [VERIFIED 2026-09-25] entry (identity
  checks: no levers = today, own-start = ~zero effect, additivity - all PASS,
  small residuals traced to display rounding of the unrounded internal
  start_rate, not a calc defect).
NOTES: reads Business Outcome's per_source directly (real_capacity.
  group_real_capacity for covered hours; labour_groups[].staff[] and
  asset_groups[].assets[] for achieved revenue). Every staff row is currently
  treated as independently leverable regardless of whether its working unit
  is asset-driven - the still-open half of step 5 (asset-driven units as ONE
  lever) will change this file's per-row leverable/basis logic once the
  asset-driven-vs-labour-driven flag is confirmed from the real selector code
  (not yet confirmed as of 2026-09-25 - do not assume a field name; candidates
  to check: asset_driven, net_asset_rate, blended_rate, labour_mix_charge_rate).

STAGE: Additive lever model
FIELD NAMES: applyAdditiveLevers() output - effects{}, total_effect,
  modelled_net_profit
FILE / FUNCTION: lib/calculations/businessModellingAdditiveModel.js
FEEDS INTO: hooks/useBusinessModellingLevers.js (state) ->
  components/business-modelling/BusinessModellingLeverPanel.jsx (display)
HOURS/UNITS AWARE: passthrough from source rows.
VERIFIED: See Business Modelling brief, [VERIFIED 2026-09-25] entry.
NOTES: pure function - effect = (target - start_rate) x basis for every row
  with a lever set; untouched rows are not recalculated, they stay at their
  achieved_revenue (rule agreed 2026-09-18/24 - touch one lever, everything
  else stays at today's real achieved figure).

STAGE: Lever persistence
FIELD NAME: qs_tools_business_modelling_levers_v1 (localStorage key)
FILE / FUNCTION: lib/storage/businessModellingLeverStorage.js;
  hooks/useBusinessModellingLevers.js (lazy useState read - has_loaded guard
  made redundant by this pattern, documented inline in the hook)
NOTES: stale row ids (a row that no longer exists in the current source list)
  are dropped from storage once the current row list is known, so switching
  data sets can't leave a phantom lever silently affecting the total.
