# QS Tools — Cost Summary Build Brief (v3.6 — NEXT MODULE)

STATUS: READY AFTER MODULE READINESS DIAGNOSTICS PASS
VERSION: v3.6

---

## CORE PURPOSE

Build the Cost Summary page as a read-only aggregation layer.

Cost Summary defines:

WHAT the business costs to operate.

It does not define:

- HOW the business recovers it
- WHAT revenue was generated
- WHAT direct costs / COGS were incurred
- WHAT profit target should be applied
- WHAT quote price should be used
- WHAT cash flow pressure exists

---

## REQUIRED UPSTREAM ORDER

P&L
↓
General Overheads
↓
Labour
↓
Assets
↓
Module Reconciliation
↓
Model Readiness
↓
Cost Summary

---

## BUILD PREREQUISITE

Do not build Cost Summary as final until:

- Module Reconciliation exists
- Model Readiness exists
- diagnostics page confirms outputs
- ModelReadinessStatusStrip exists
- Labour final productive output is confirmed
- Assets forward-looking interest logic is confirmed
- General Overheads interest handling is confirmed

---

## INPUTS

### From Labour

- total_labour_cost_annual
- total_gross_wages_annual
- total_entitlements_annual
- total_employer_kiwisaver_annual
- total_esct_annual
- total_acc_levy_annual
- total_employer_contribution_annual
- total_productive_output

### From Assets

- total_asset_cost_annual
- total_asset_interest_annual where available for supporting display only

### From General Overheads

- total_general_overheads

### From Model Readiness

- model_ready
- blocking_modules
- warning_modules
- blocking_checks
- warning_checks
- model_readiness_status

---

## REMOVED INPUTS

Cost Summary must not consume:

- employee_overheads_total
- total_employee_overheads_annual
- employee_overheads_total_annual
- employee_overheads_ready
- staff_overheads_total
- active_revenue_model
- Cost Allocation outputs
- Recovery Summary outputs
- Scenario outputs
- Revenue / COGS outputs
- Business Summary outputs
- Business Modelling outputs
- raw P&L lines
- P&L benchmark values as calculation inputs
- P&L interest as asset finance authority
- historical P&L asset interest
- running_cost_annual
- principal repayments

---

## FORWARD-LOOKING BUILD RULE

Cost Summary must be built as a forward-looking operating cost page.

It must not try to recreate the previous P&L period.

It must use current / forward-looking outputs from upstream modules.

Past P&L values may support reconciliation and setup only.

They must not override Labour, Assets, or General Overheads output contracts.

---

## ASSET INTEREST BUILD RULE

Assets is the authority for asset finance interest.

Cost Summary must consume asset ownership cost from Assets.

Cost Summary must not calculate asset interest directly.

Cost Summary must not use P&L interest as the source of truth for asset finance.

If the P&L indicates that interest includes asset finance, that setting belongs to reconciliation / overhead interpretation only.

Cost Summary still consumes asset finance interest from Assets.

---

## CASH FLOW SEPARATION BUILD RULE

Principal repayments must not feed Cost Summary.

Principal repayments are cash-flow pressure, not operating cost.

Do not include principal in:

- total_asset_cost_annual
- total_business_cost_annual
- total_cost_burden
- required_recovery_rate

Future Cash Flow layer may consume principal-related outputs separately.

---

## CALCULATIONS

total_people_cost_annual =
total_labour_cost_annual

total_business_overheads =
total_general_overheads

total_business_cost_annual =
total_asset_cost_annual + total_business_overheads

total_cost_burden =
total_people_cost_annual
+ total_asset_cost_annual
+ total_business_overheads

required_revenue =
total_cost_burden

required_recovery_rate =
required_revenue / total_productive_output

If total_productive_output = 0:

required_recovery_rate = 0
warning required.

---

## PRODUCTIVE OUTPUT RULE

Cost Summary must use Labour’s final productive output.

This must be the post-entitlement, post-productivity output.

Cost Summary must not use:

- paid hours
- raw working hours
- pre-productivity hours
- estimated hours created inside Cost Summary

If total_productive_output is missing or zero, Cost Summary must warn and set required_recovery_rate to 0.

---

## DISPLAY STRUCTURE

Cost Summary Card:

1. Model Readiness Status Strip
2. Headline Summary
   - Total Operating Cost
   - Total Productive Hours
   - Required Recovery Rate
3. People Cost
4. Asset Cost
5. General Overheads
6. Total Business Cost
7. Total Cost Burden
8. Required Revenue
9. Required Recovery Rate

---

## HEADLINE DISPLAY

The first visible section should prioritise:

- Total Operating Cost
- Total Productive Hours
- Required Recovery Rate

Required Recovery Rate must be visually clear.

This is the key result of Cost Summary.

---

## DRILLDOWN DISPLAY

Cost Summary must be macro-first.

Detailed components may be shown inside expandable / drilldown sections.

People Cost drilldown may show:

- Gross Wages
- Entitlements
- Employer KiwiSaver
- ESCT
- Employer ACC Levy
- Employer Contribution Total

Asset Cost drilldown may show:

- Total Asset Cost
- Total Asset Interest where available
- Asset ownership detail where available

General Overheads drilldown may show:

- General Overheads total
- Other / non-asset interest where available

Do not place asset interest and other interest as primary top-level lines.

They are supporting detail.

---

## SECONDARY VIEW

Cost Composition:

- Labour %
- Assets %
- General Overheads %

This view is display-only.

It must not alter calculations.

---

## READINESS BEHAVIOUR

If model_ready = false:

- totals may display
- page must show not trusted state
- downstream modelling is blocked
- blocking_modules must be visible
- warning_modules must be visible

If model_ready = true and model_readiness_status = warning:

- totals may display as usable
- warnings remain visible

If model_ready = true and model_readiness_status = ready:

- totals are trusted

---

## RECONCILIATION

Cost Summary may display reconciliation status.

Cost Summary must not own reconciliation.

P&L benchmark totals must not drive Cost Summary calculations.

If mismatch exists:

- show warning / error state from Module Reconciliation
- do not silently ignore
- do not alter Cost Summary totals

Cost Summary must not attempt to reconcile historical P&L asset interest to forward-looking asset interest.

That conflict is informational only.

---

## UI REQUIREMENTS

- Clean summary card
- Strong emphasis on Total Operating Cost
- Strong emphasis on Required Recovery Rate
- Total Productive Hours visible
- No input fields
- Read-only aggregation layer
- Readiness warning visible
- Drilldown available for supporting detail
- No recovery strategy controls
- No pricing controls
- No capacity controls
- No cash flow controls

---

## PAGE PRINCIPLE

Cost Summary must feel like:

Result first.
Detail on demand.

The user should immediately understand:

- what the business costs to run
- how many productive hours carry that cost
- what recovery rate is required

The user should not need to inspect every detail to understand the result.

---

## NEXT STEP AFTER THIS

After Cost Summary is trusted, build:

1. Revenue / COGS
2. Business Summary
3. Business Modelling
4. Quote Engine later
5. Cash Flow layer later

Recovery Summary / Pricing engine references should be replaced by the newer Revenue / COGS → Business Summary → Business Modelling flow unless a later locked brief reinstates Recovery Summary.

---

## FINAL DEFINITION

Cost Summary Build v3.6 creates a read-only, forward-looking aggregation page that consumes trusted upstream module totals and the Model Readiness verdict.

It produces total operating cost burden, total productive output, and required recovery rate.

It does not perform reconciliation, pricing, allocation, cash-flow modelling, or recovery strategy.