P&L MODULE COMPLETION — 2026-04-20 (UPDATED)

The Profit & Loss page has been fully refactored and stabilised.

WHAT WAS DONE
Broke down oversized MainCard into modular components
Cleaned up UI structure and removed duplication
Implemented clear separation of concerns
Fixed Save logic (period-based overwrite)
Removed snapshot/profile workflow (premature complexity)
Simplified user flow to Reset + Save
Added clear "Editing" context in header
CURRENT STATE

The P&L page now behaves as:

a single working P&L model
with simple save functionality
no profile switching
no multi-period UI

This is intentional.

WHY THIS APPROACH

The previous version introduced complexity too early:

multiple profiles
snapshot logic
unclear user flow

These have been removed to:

simplify the UX
stabilise the system
align with current capability
IMPORTANT DESIGN DECISION

We are NOT supporting:

multiple P&L profiles
multi-period comparison
historical navigation

at this stage.

These will be introduced later once:

Cost Summary
Recovery
Allocation

are fully working.

SYSTEM ROLE (CLARIFIED)

P&L is a:

classification layer
benchmark reference
setup guidance layer

It does NOT:

perform cost calculations
replace downstream modules
feed directly into Cost Summary logic
WHAT THIS ENABLES

We now have:

a clean input layer
reliable data structure
predictable behavior
stable foundation for downstream modules
NEXT STEP

Begin Employee Overheads module build.

P&L will later be used for:

benchmarking
reconciliation against model outputs
NOTE

This is a deliberate simplification phase.

Complexity will be reintroduced later in a controlled way.

END
:::