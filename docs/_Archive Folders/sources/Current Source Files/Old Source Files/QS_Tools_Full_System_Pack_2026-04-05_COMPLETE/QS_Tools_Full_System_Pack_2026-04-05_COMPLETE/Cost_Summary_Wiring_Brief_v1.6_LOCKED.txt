# QS Tools — Cost Summary Wiring Brief (v1.6 — LOCKED)

## STATUS

IMPLEMENTATION LOCKED — BUILD READY

## OBJECTIVE

Build the Cost Summary wiring layer so Cost Summary consumes locked upstream output contracts exactly as provided.

This phase is about:

- wiring live upstream hook outputs into Cost Summary
- shaping outputs inside Cost Summary selectors only
- calculating Cost Summary totals from locked upstream outputs
- surfacing structure warnings and counts
- keeping `page.jsx` orchestration-only

## FINAL RULE

Cost Summary is not a guessing layer.

It is an aggregation layer.

## EMPLOYEE OVERHEADS WIRING RULE

Cost Summary must consume from Employee Overheads:

### Per staff
- `staff_id`
- `employee_overheads_total_annual`

### Aggregate
- `total_employee_overheads_annual`

Cost Summary must NOT:

- derive staff overhead totals from category inputs
- rebuild Employee Overheads maths
- guess unsaved overhead records

## SUCCESSIVE MODULE RULE

This same pattern must be used for all later upstream modules:

- module owns outputs
- Cost Summary consumes outputs
- Cost Summary aggregates only
