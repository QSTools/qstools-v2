# Review Note — Labour Output Contract Follow-Up Pending

This aligned update pack resolves the downstream version drift and the Cost Summary / Budget naming mismatches.

The remaining contract item to resolve separately is the Labour-side People Cost split required by Cost Summary.

Still pending against the locked Labour contract:

- whether Labour must expose the additional output(s) needed for Cost Summary to calculate:
  - `gross_wages_annual`
  - `entitlements_annual`

Reason:

Current Cost Summary structure requires separate People Cost visibility for:
- Gross Wages
- Entitlements
- Employer KiwiSaver
- ESCT
- Employee Overheads

Current locked Labour outputs already provide:
- `paid_hours_per_year`
- `non_productive_paid_hours`
- `employer_kiwisaver_gross`
- `esct_amount`

But they do not yet expose the additional wage-basis output needed for downstream Gross Wages / Entitlements split without rebuilding Labour internals.

So this pack is aligned for the non-Labour tidy-up pass, while the Labour output contract remains the next explicit decision.
