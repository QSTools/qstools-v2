Yes — Recovery Summary is now the locked next-layer baseline.

## Recovery Summary locked state

**Module:** Recovery Summary
**Version:** v1.0
**Role:** commercial distribution layer
**Position:** Cost Summary → Recovery Summary → Cost Allocation

## What is now locked

* consumes **Cost Summary outputs only**
* does **not** read raw Labour / Assets / General Overheads state
* does **not** rebuild upstream maths
* owns:

  * `recovery_model`
  * `labour_share_percent`
  * `asset_share_percent`
  * `overhead_share_percent`
* produces:

  * `active_recovery_model`
  * `labour_recovery_cost`
  * `asset_recovery_cost`
  * `overhead_absorbed_cost`
  * `required_labour_recovery_rate`
  * `required_asset_recovery`

## Locked page structure

1. `RecoverySummaryStatusStrip`
2. `RecoverySummaryMainCard`
3. `RecoverySummaryHelpPanel`

## Locked rule clarity

* **Cost Summary = WHAT it costs**
* **Recovery Summary = HOW it is recovered**
* **Cost Allocation = CAN it be delivered**

## Correct next move

Now we move to **Cost Allocation v2.1**, not back into Recovery.
That matches the locked implementation order.

## Practical instruction

Treat Recovery Summary as:

* stable contract
* stable page order
* stable variable names
* stable ownership boundary

Only bug fixes or wording cleanup from here unless the brief changes.


