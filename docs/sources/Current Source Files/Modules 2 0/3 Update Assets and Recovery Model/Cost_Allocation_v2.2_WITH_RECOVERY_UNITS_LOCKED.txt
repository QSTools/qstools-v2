Cost_Allocation_v2.2_WITH_RECOVERY_UNITS_LOCKED.txt

QS Tools — Cost Allocation (v2.2 — LOCKED)

STATUS
UPDATED — RECOVERY UNIT MODEL

PURPOSE
Defines CAN the business deliver recovery.

CORE CHANGE

Operational groups now represent real recovery units.

GROUP CONTRACT

{
group_id,
group_name,
required_asset_ids[],
required_staff_ids[],
is_active
}

UNIT DEFINITION

Recovery Unit =
Asset(s) + Staff

Examples:

digger + driver
pump + operator

UNIT ECONOMICS (DERIVED INTENT ONLY)

unit_cost_per_hour =
sum(asset cost per hour) +
sum(labour cost per hour)

RULES

only productive assets allowed
only production labour allowed
support assets excluded

VALIDATION

Structure is valid only if:

assets assigned
staff assigned
groups exist

DO NOT

calculate costs inside Cost Allocation
duplicate labour or asset maths
override Recovery Summary

FINAL RULE

Cost Allocation defines structure only — not cost or strategy.

STATUS

LOCKED — READY FOR UPDATE