Volume Rate Page — UI Layout Map v1.0
Page purpose

For tonne / m³ / load-based work.

File structure

components/rates/volume/

VolumeRateStatusStrip.jsx
VolumeRateMainCard.jsx
VolumeRateSummaryCard.jsx
VolumeRateHelpPanel.jsx
Page order
VolumeRateStatusStrip
VolumeRateMainCard
VolumeRateSummaryCard
VolumeRateHelpPanel
1. VolumeRateStatusStrip

Show:

unit_type
base_unit_rate
minimum_charge
expected_annual_units
expected_annual_revenue
2. VolumeRateMainCard
Section A — Page intro

Short explanation:
“This page models revenue generated from tonne-, m³-, or load-based pricing.”

Section B — Core volume inputs

Fields:

unit_type
base_unit_rate
minimum_charge
expected_annual_units
Section C — Pricing behaviour

Collapsed by default.

Fields:

small_job_threshold_units
small_job_uplift_percent
large_job_discount_percent
average_job_size_units
Section D — Optional included basis labels

Display-only or light-input:

included_asset_basis
included_labour_basis
included_material_basis
Section E — Revenue preview

Read-only:

rate_model_type
unit_label
expected_annual_volume
expected_revenue_annual
expected_recovery_contribution
Section F — warnings

Examples:

no unit type selected
base rate missing
annual volume missing
very low expected production
3. VolumeRateSummaryCard

Headline values:

Rate per unit
Minimum Charge
Expected Annual Volume
Expected Annual Revenue
Optional insight line

Examples:

“Small jobs require materially higher effective rates.”
“Large job pricing reduces unit recovery pressure.”
“This model depends on annual throughput.”
4. VolumeRateHelpPanel

Collapsed by default.

Explain:

tonne vs m³ vs load
minimum charge logic
small job vs large job behaviour
this page is production modelling, not dispatch or scheduling