QS Tools — UI Migration Plan (v1.1 — LOCKED)

---

PURPOSE

This brief defines how existing QS Tools pages are migrated to the shared UI system.

It ensures:

* pages are rebuilt once only
* UI consistency across the system
* no disruption to business logic
* stable rollout across modules

---

CORE PRINCIPLE

Rebuild pages onto the shared UI system without changing logic.

---

SCOPE

This migration applies to:

* all pages using legacy or mixed styling
* all future page builds

---

WHAT THIS MIGRATION DOES

* replaces page-level styling with shared UI classes
* standardises layout structure
* aligns pages to mobile-first behaviour
* prepares the system for future visual updates through globals.css

---

WHAT THIS MIGRATION DOES NOT DO

* change calculations
* change module behaviour
* change data contracts
* change ownership boundaries
* redesign business logic
* restructure page flows

---

LOCKED BASELINE

Migration must use:

* shared UI classes from UI Implementation Standard v1.1
* current globals.css as the single design source

Current locked CSS baseline includes:

* theme tokens
* page shell classes
* shared card/panel/input/button classes
* typography helpers
* demo/homepage helper classes
* labour-layout
* cost-summary-layout

---

MIGRATION RULES

1. Replace page styling with shared UI classes
2. Do not create new styling systems
3. Do not mix old and new systems inconsistently
4. Preserve all existing logic and data flow
5. Preserve page order and structure
6. Use approved layout helpers only

---

PAGE MIGRATION PATTERN

Each page must be rebuilt using:

<main className="ui-page">
  <div className="ui-page-stack">
    ...
  </div>
</main>

Then apply:

* ui-section for major blocks
* ui-panel for inner cards
* shared inputs, buttons, and spacing
* approved specialised layouts only where already locked

---

RESPONSIVE MIGRATION RULE

Mobile

* vertical stacking
* no forced columns

Tablet

* controlled ui-split-2 usage only

Desktop

* approved layouts only
* no custom grid systems
* no right-rail hacks outside approved layout helpers

---

ORDER OF MIGRATION

Phase 1 — Homepage

* validate shared UI system
* confirm spacing, typography, and layout baseline

Phase 2 — Simple pages

* pages with minimal interaction
* apply the standard shell and card system

Phase 3 — Core modules

* Labour
* Cost Summary
* P&L
* other primary system pages

Phase 4 — Downstream pages

* Recovery Summary
* Cost Allocation
* Recovery Outcome
* later summary/validation pages

---

VALIDATION AFTER EACH PAGE

Each migrated page must be checked for:

* correct UI class usage
* no inline styles
* no duplicate styling systems
* responsive behaviour
* unchanged logic

---

BRIEF UPDATE RULE

After migration:

* update that page brief to the next version
* confirm shared UI system usage
* confirm no logic changes

Examples:

* v1.0 → v1.1
* v3.0 → v3.1

Do not perform a full brief rewrite across the whole system before rebuild.

Global briefs remain locked.
Page briefs are updated as each page is rebuilt.

---

FAILURE CONDITIONS

Migration fails if:

* pages introduce custom styling systems
* logic is altered unintentionally
* layout becomes inconsistent across pages
* responsiveness is broken
* shared UI classes are bypassed in favour of one-off styling

---

SUCCESS CRITERIA

* all rebuilt pages use shared UI classes
* consistent layout across modules
* no duplicated UI logic
* visual updates remain possible via globals.css only
* pages do not need another structural UI pass later

---

FINAL DEFINITION

This migration locks the transition from page-level styling to the shared globals.css UI system, ensuring QS Tools can be rebuilt page by page without requiring another full UI migration later.
