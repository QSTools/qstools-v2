# Mirra v5.0 - Home Route / Landing Flow Decision Brief

STATUS: DECISION BRIEF - MIGRATION ALIGNMENT
VERSION: v5.0
MODULE TYPE: Route Flow / Handover Alignment / No-Code Decision
DATE: 2026-07-15

---

# 1. PURPOSE

S10 records the home route and landing flow decision before Pro migration.

The goal is not to expand product scope.

The goal is to make the current route flow clear for handover and migration.

---

# 2. CURRENT STATE

The app now has a complete discoverable flow:

- /business-overview
- /model-readiness
- /business-modelling
- /quote-checker
- /ai-business-state
- /calculation-trace

Navigation has been updated so the main user-facing flow is accessible through the Business Review group.

---

# 3. DECISION

The root route / will remain unchanged for now.

Business Overview will not be forced as the root route before migration.

No redirect will be added before migration.

No home route rewrite will be added before migration.

---

# 4. REASON

Before Pro migration, the safest approach is to avoid changing app entry behaviour unless required.

Business Overview is already discoverable in the sidebar navigation.

Quote Checker is already discoverable in the sidebar navigation.

The current route structure is sufficient for handover.

Changing / to redirect or render /business-overview can be done later if needed.

---

# 5. LOCKED USER FLOW FOR MIGRATION

Primary user-facing flow:

1. Business Inputs
2. Business Truth
3. Business Review
4. Quote Checker

Main Business Review routes:

- /business-overview
- /model-readiness
- /business-modelling
- /quote-checker

Developer / trace routes:

- /ai-business-state
- /calculation-trace

Legacy / older routes remain discoverable but should not drive new build decisions unless explicitly selected.

---

# 6. CODE CHANGE DECISION

No code change is required for S10.

No route files are changed.

No navigation files are changed.

No output contracts are changed.

No source modules are changed.

---

# 7. LOCKED BOUNDARIES

S10 must not:

- rewrite the home route
- redirect /
- alter navigation groups
- calculate business values
- alter source modules
- alter output contracts
- change Model Readiness
- change Business Modelling
- change AI Export
- change Quote Checker

S10 may only:

- document the home route decision
- document the landing flow
- document migration-safe route behaviour

---

# 8. HANDOVER NOTE

For migration and future build:

Business Overview is the preferred operational starting point.

The root route / has not been changed so the migration baseline remains low risk.

Future decision:

- keep / as a separate home page
- render Business Overview at /
- redirect / to /business-overview

This decision is deferred until after migration.

---

# 9. VALIDATION REQUIREMENT

Because S10 is documentation-only, validation requires:

- brief created
- brief committed
- git status clean

No build is required unless another code file changes.

---

# 10. FINAL LOCK STATEMENT

S10 Home Route / Landing Flow decision is locked for migration.

The app root route remains unchanged.

Business Overview remains the preferred operational starting point but is not forced as the root route before migration.

---

# 11. FINAL PRINCIPLE

The landing flow should guide the user without risking the migration baseline.
