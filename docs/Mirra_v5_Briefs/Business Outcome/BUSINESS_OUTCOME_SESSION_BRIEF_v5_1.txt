# Business Outcome - Session Brief (Running, updated 2026-09-12)
**Repo:** D:\QSTools\qstools-web
**Supersedes:** all prior chat-only versions of this brief - this is the first time it has existed as a real, saved file. Prior sessions (2026-09-10, 2026-09-11) only existed as chat-pasted content and are summarised here from that history.
**This is the single document to work from next session.**

---

## 0. Quick orientation - start here in a new chat

Read this whole document before doing anything. Also read BUSINESS_OUTCOME_FILE_MAP_v5_1.txt (same folder) for fast file/function lookup - this brief is the narrative, the file map is the reference.

**What's solid and shipped, across all sessions to date (don't re-verify from scratch):**
- The blurb (headline, View A/B resolution, variance explanation, cross-subsidy naming)
- Full trace-and-verify pass across the page's core calculation logic (2026-09-10/11, ten calculation patterns, zero bugs found)
- Capacity Coverage Gap feature (calculation + table + blurb integration)
- Health Gauge - baseline calculation, visual UI (arc + needle), AND (new 2026-09-12) clickable explainer panel with legend and cross-subsidy naming
- CFO Requirements Gap document - scoping only, not a build, but a real saved reference for future CFO-readiness work

**What's still open, in priority order:**
1. Outcome -> Modelling handoff fields - not yet confirmed.
2. [CLOSED 2026-09-12] Verify Projected Net Profit (BusinessOutcomeRevenueSnapshotTable.jsx) is genuinely complete - CONFIRMED complete and correct. Formula is modelled_revenue - breakeven_revenue, both independently-verified inputs. Call-site wiring confirmed correct (modelled_revenue -> per_source.headline_real_capacity.independent_modelled_revenue, NOT the total_modelled_revenue P&L alias; breakeven_revenue -> per_source.reconciliation.total_true_cost, already validated in the original trace-and-verify pass). No code changes needed - verification only.
3. Business Modelling implementation itself - the big next phase. Architecture decided (Section 2.5 below), nothing built yet.
4. [CLOSED 2026-09-12] card2_view_mode_ab - RESOLVED, not a bug. This is the "Materials model (Card 2 override)" toggle: a deliberate, independent View A/B selector local to the group-level ranked drill-down section (switches between RankedGroupsDrill and ViewBGroupsDrill), separate from the page-level view_mode_ab toggle used by the blurb/headline. One-way sync: starts matched to the main toggle, decouples once the user clicks it directly, re-syncs if the main toggle changes again. Does NOT need the blurb's View-A-stability treatment - unlike the blurb (a single narrative that shouldn't flicker), this toggle's entire purpose is letting the user choose which view they're looking at in this specific drill-down, including the View-B-only Rate/Hours Shortfall breakdown. Already well-commented in the code (BusinessOutcomePerSourceRevenueCard.jsx, search "Card 2 gets its own copy"). No code change needed - investigation only.
5. [SHIPPED 2026-09-12] Balance sheet import (Xero) - DONE, see Section 6 below for full build detail.
6. Revenue/COGS trend view - buildable now, no blockers, per CFO gap doc.
7. Labour/Asset historical tracking - needed for full trended profitability. Likely a large change, comparable to the Asset Utilisation/Downtime prerequisite. Not started, not scoped.
8. EBITDA-by-unit / formal segment reporting - needs its own scoping session, depends on confirming per-source depreciation/amortisation/interest data availability.

**Standing conventions still apply** - see "Working_Conventions - PowerShell Patching, CSS, and Numeric Verification" at repo root: PowerShell one command at a time, targeted Apply-Patch (IndexOf/Substring) patches only, .NET UTF-8-no-BOM file I/O, build after every patch, five specific pre-existing files to leave untouched (see Section 7 of the file map).

---

## 1. Session 2026-09-12 - what shipped (all committed, built, live-tested)

| # | Change | Commit |
|---|---|---|
| 1 | lib/utils/scrollToSection.js extracted - shared navigation engine, reused by both ScrollToSectionLink and the new Health Gauge click-through | bd786f4 |
| 2 | ScrollToSectionLink.jsx refactored to delegate to the shared util - no behavior change | bd786f4 |
| 3 | BusinessOutcomeHealthGauge.jsx - exports SCALE_MIN, SCALE_MAX, zone_for_ratio so the legend can never drift from the actual gauge | bd786f4 |
| 4 | New BusinessOutcomeHealthGaugeExplainer.jsx - explanation, two headline numbers as metric cards, link to real per-source proof table, 3-zone legend, cross-subsidy N-of-M line | bd786f4 |
| 5 | New CSS: legend table/row/header/dot classes + clickable-gauge button reset, in business-outcome.css | bd786f4 |
| 6 | Gauge wrapped in clickable button, navigates to new explainer panel under "How the Numbers Are Calculated" | bd786f4 |
| 7 | Cross-subsidy named consistently: View B card, blurb all-healthy line, blurb worst-source line (gauge explainer already had it) | 8871c4f |
| 8 | CFO Requirements Gap document written and saved - Part 1 (already correct), Part 2 (real but unlabeled), Part 3 (genuinely missing, feasibility-assessed), Part 4 (recommended build order if pursued) | fa54b8e |
| 9 | File Map saved as a real file for the first time (previously chat-only) | (docs, uncommitted separately from code, see file list) |
| 10 | This brief saved as a real file for the first time (previously chat-only) | (this commit) |

**Methodology notes from this session, worth carrying forward:**
- PowerShell here-strings MUST use single-quoted delimiters (@' ... '@) when content includes JS template literals with ${...} - double-quoted here-strings (@" ... "@) make PowerShell misinterpret ${...} as its own variable syntax and throw a parser error. Bit once this session (format_currency helper).
- A "0.00 in amber" gauge reading mid-session was NOT a bug - confirmed via brace-count check, prop-wiring check, and CSS inspection that everything was structurally sound. Root cause was the live underlying revenue input being at a value that genuinely produced an undefined/zero ratio at that moment - changing the input resolved it immediately to a correct 0.96. Lesson: a 0.00 gauge display isn't automatically a code bug - check live input state first.
- --warning CSS variable genuinely exists (defined across all theme variants in app/globals.css) - an older note in the Working Conventions doc claiming it doesn't exist is now outdated.
- businessModellingIndependentCalculations.js remains on the "leave untouched" list - confirmed 2026-09-12 that if new gauge/CFO-related calculation is ever needed, create a NEW file rather than extending this one.

---

## 2. CFO-readiness context (new as of 2026-09-12, see CFO_REQUIREMENTS_GAP_2026-09-12.txt for full detail)

**Why this matters:** Business Outcome is intended for eventual CFO adoption. Correct standard terminology matters for adoption - a CFO needs to recognise real, standard concepts (contribution margin, cross-subsidy, segment reporting) even where Mirra's own numbers are bespoke.

**Confirmed already correct (not just labeled, genuinely built right):** contribution margin toggle, Gross Margin %, Net Operating Margin %, markup-vs-margin distinction, and (as of this session) cross-subsidy naming.

**Confirmed missing, feasibility-assessed:**
- Working capital / liquidity ratios - [SHIPPED 2026-09-12] DONE, see Section 6.
- Period-over-period trend - PARTIALLY feasible now (Revenue/COGS has 12 months of dated Xero data on every import already). FULLY blocked on Labour/Asset hours getting historical/dated tracking, which they do not have today (current-state snapshots only).
- EBITDA-by-unit and formal segment reporting - not yet scoped, depends on confirming whether per-source depreciation/amortisation/interest data exists or can be added.

**Live data design lens (user instruction, 2026-09-12):** the system will eventually run on live, continuously-updating Xero feeds rather than periodic manual snapshots. Anything built from here on - especially trend/period work - should be designed so it would naturally extend to a live feed, not hard-assume a one-off annual snapshot that would need unpicking later.

---

## 2A. Business Modelling architecture - MAJOR CORRECTION (found 2026-09-12)

Prior versions of this brief claimed Business Modelling architecture was "decided, nothing built yet" (see old Section 3.4/2.5 language, now corrected below). This was WRONG - confirmed via git log and direct file read 2026-09-12. Do not re-assume "nothing built" without checking this section first.

**Reality: TWO separate, coexisting systems currently feed the Business Modelling page:**

**System 1 - legacy baseline/scenario (older):**
- Fed from useBusinessSummary() - Business Summary, NOT Business Outcome.
- Builds baseline_snapshot, upside_scenario, downside_scenario via buildBaselineSnapshot/buildScenarioState (lib/calculations/businessModellingCalculations.js).
- Persisted to storage (lib/storage/businessModellingStorage.js).
- Rendered on the page inside a CollapsibleSection explicitly titled "Previous scenario tools", summary text "Legacy baseline / upside / downside controls - being replaced", defaultOpen={false}.
- Still fully wired and running every render even though visually collapsed - not dead code, genuine ongoing dependency.

**System 2 - live lever architecture (newer, built early September, commit f98b9f3 "S31 Phase 2-3"):**
- Fed DIRECTLY from useBusinessOutcomePerSourceRevenue() + selectBusinessOutcomePerSourceRevenue() - this IS genuinely the Outcome-to-Modelling handoff.
- Builds live_headline, lever_rows, materials_lever_row, proportional_suggestions via lib/selectors/businessModellingLeverSelectors.js - real-time, no storage/save-load.
- Rendered as BusinessModellingLeverCard, the PRIMARY always-visible content at the top of the page.
- This is what Section 3.4's "reuse the engine" architecture principle actually describes, and what S30's Sections 3-4 ("independent per-group targets", "materials/COG independent target + lever") were building toward - already built, not still-pending as previously assumed.
- The Health Gauge (built 2026-09-11/12) is very likely S30 Section 12's candidate metric #3 ("ratio between smoothed-zero revenue and independent-standalone revenue... DEPENDS ON Sections 3-4 existing") - buildable specifically because this system already existed.

**What "Outcome -> Modelling handoff fields, confirm/finalize" actually means, now that this is understood:** the handoff itself already works correctly and is live in production (System 2).

**[DECIDED 2026-09-12] System 1's fate:** NOT a straight retire. Full redesign decision locked - see BUSINESS_MODELLING_SCENARIO_REDESIGN_DECISION_LOCK_2026-09-12.txt (same folder) for complete detail. Summary: keep the three-scenario concept (Baseline/Upside/Downside), rebuild it entirely on System 2's live-lever engine instead of the old useBusinessSummary source. Baseline becomes a periodic locked snapshot (quarterly suggested) of real Outcome data, not continuously editable. Upside becomes a saved, editable target scenario using real levers. Downside is completely redefined - no longer a manually-edited pessimistic guess, now a CALCULATED breakeven reference line positioned relative to Baseline (below Baseline if profitable, above if in loss). Old system's code (businessModellingCalculations.js/businessModellingSelectors.js/businessModellingStorage.js) stays in place as a safety net until the rebuild is verified working - not yet built, this is a decision lock only, sized as its own future dedicated session.

**Cleanup done 2026-09-12:** removed a leftover debug artifact (window.__proportional_debug, commented "TEMPORARY - Phase 2 verification only. Remove before final commit.") that had shipped to production since the S31 commit. Commit 5b4be54.

**businessModellingIndependentCalculations.js status clarified:** this file (on the "leave untouched" list) is System 2's actual calculation engine, not a stray/unused file - the "S31 Phase 2-3" commit added 254 lines to it. Its untouched-list status is because it's active, load-bearing, already-verified code, not because it's abandoned.

---

## 2B. Balance Sheet import - SHIPPED (2026-09-12)

Full new module built and live-tested against a real Xero Balance Sheet export, this session. Closes the "working capital / liquidity ratios" gap flagged in CFO_REQUIREMENTS_GAP_2026-09-12.txt Part 3.

**Seven commits, full stack:**
- lib/imports/xeroBalanceSheetImporter.js - parses Xero's Balance Sheet Excel export. Verified against a real file: 2 bugs found and fixed (Net Assets/Total Equity misclassified as an account line since they don't start with "Total "; a false warning on the "Business Name" metadata row). Cross-validated correct via Net Assets = Total Equity matching to the penny.
- lib/storage/balanceSheetStorage.js - localStorage persistence, follows revenueCogsStorage.js pattern.
- lib/calculations/balanceSheetCalculations.js - working_capital, current_ratio, quick_ratio, debt_to_equity. Verified against real data AND edge cases (empty input, zero-denominator division guard).
- hooks/useBalanceSheet.js - ties storage/import/calculations together. ONE REAL BUG found and fixed: a save-before-load race condition was silently wiping imported data on every page navigation (the save effect ran before the load effect's setState had flushed, overwriting real data with the empty default) - fixed with a has_loaded guard.
- components/balance-sheet/ - BalanceSheetImportPanel (file upload), BalanceSheetRatiosCard (4 metric cards), BalanceSheetAccountsTable (full account breakdown, collapsible by subsection via the existing CollapsibleSection component, summary total shown even when collapsed), BalanceSheetHelpPanel (plain-English explanation of what each ratio means, added after user feedback that raw ratios aren't self-explanatory), BalanceSheetSituationSummary (live narrative blurb reacting to the actual computed numbers - matches BusinessOutcomeTruthSituationSummary.jsx's pattern, not a fixed template).
- app/balance-sheet/page.jsx - full page, added to sidebar nav under "Business Inputs" (same group as P&L and Revenue/COGS).

**Real result against the actual test balance sheet (as at 31 March 2026):** working capital -$54,035, current ratio 0.80, quick ratio 0.75, debt-to-equity 5.35 - a genuine liquidity concern, not a clean test case. The live narrative correctly flags this rather than presenting it neutrally.

**Still Excel-file import, not a live Xero API pull** - API access needs a Xero trial/OAuth app, not yet set up. The importer's output shape was deliberately designed to match what a future live API pull would return, so switching to live data later should mean replacing this file's internals, not reshaping every downstream consumer (same "live data" design lens as CFO_REQUIREMENTS_GAP_2026-09-12.txt's guidance).

**One thing flagged, not yet built:** cash ratio specifically. The real test export didn't have a clean, identifiable "cash and cash equivalents" line - bank account balances appeared under Current Liabilities, not Current Assets, in the actual file (worth investigating real chart-of-accounts conventions before building this, rather than guessing at account-name matching that could be misleading).

---

## 3. View A vs View B - the full resolved picture (carried forward from 2026-09-10/11, still valid)

### 3.1 The mechanism
- View A's materials revenue is a residual: total_revenue - labour_modelled - asset_modelled. Forced by construction.
- View B's materials revenue is independent: total_cogs x (1 + markup%) - anchored to real COGS and Rate Builder's markup.
- Confirmed intentional, not a bug. Decision: keep View B's independent basis, do not force reconciliation.

### 3.2 Which view feeds Business Modelling - resolved
View A is the trusted base for all lever math. View B's role is a check-and-balance overlay for the materials lever specifically, not a modelling input.

### 3.3 Confirmed lever set for Business Modelling (five types, all View A)
1. Overhead cost change
2. Rate change
3. Labour cost/productivity change
4. Asset cost/utilisation change
5. Material markup sensitivity (View B's check-and-balance role attaches here)

### 3.4 Architecture principle - reuse the engine, don't rebuild it
Business Modelling should not have its own copy of the calculation chain. A lever input runs back through Business Outcome's SAME chain via a "calculation context" input bundle - real data fills it normally, Modelling fills an identically-shaped bundle with modelled values, same functions run against either. True size unknown until scoped against actual code - a next-session, at-the-computer task. Not yet scoped.

---

## 4. Lessons learned - worth remembering (accumulated across all sessions)

- The blurb has two separate prop lists that must stay in sync (Card JSX call, wrapper destructuring, wrapper's forwarding call into build_situation_summary). Miss the middle step and the paragraph silently doesn't render.
- Field-name traps: per_source.labour_groups[]/asset_groups[] use .group_modelled_revenue/.group_true_cost, NOT .modelled_revenue/.total_cost. per_source.materials uses .revenue, NOT .modelled_revenue. View B's raw shapes use .modelled_revenue directly - View A and View B are NOT parallel. Always grep for an existing working consumer before writing new code that reads a field.
- Every ScrollToSectionLink (or direct scroll_to_section() call) into "How the Numbers Are Calculated" needs pre_toggle_labels: ["Show breakdown"] - that whole area is gated behind a plain button first, not just nested CollapsibleSections.
- The per_source selector explicitly lists every field it returns - a new field on the hook's raw return object will NOT reach the Card unless also added in businessOutcomePerSourceRevenueSelectors.js.
- Per-source direct/overhead cost IS genuinely tracked separately (direct_cost, overhead_share, summed to true_cost) - confirmed 2026-09-12, this is real data any future CFO-metric work can build on honestly.

---

## 5. Standing conventions (unchanged, reaffirmed)

Full detail: "Working_Conventions - PowerShell Patching, CSS, and Numeric Verification" at repo root. Summary: PowerShell one command at a time, exact Apply-Patch (IndexOf/Substring) targeted edits, .NET UTF-8-no-BOM file I/O only, full build after every single patch, single-quoted PowerShell here-strings for any JS/JSX content, five pre-existing files remain untouched unless specifically working on them (confirm via git status): components/business-modelling/BusinessModellingLeverCard.jsx, components/layout/SidebarNavigation.jsx, docs/Mirra_v5_Briefs/S22 - ASSET FINANCE INTEREST EXCLUSION & COVERAGE CHECK.txt, lib/calculations/businessModellingIndependentCalculations.js, lib/selectors/general-overheads/generalOverheadCategorySelectors.js.

Run git log --oneline -15 to confirm this commit history is current before starting new work.