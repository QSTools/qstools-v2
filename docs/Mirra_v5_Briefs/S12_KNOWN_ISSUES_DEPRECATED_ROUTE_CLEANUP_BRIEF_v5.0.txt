# Mirra v5.0 - Known Issues / Deprecated Route Cleanup Brief

STATUS: MIGRATION CONTROL BRIEF - READY
VERSION: v5.0
MODULE TYPE: Known Issues / Deprecated Route Control / Migration Guardrail
DATE: 2026-07-15

---

# 1. PURPOSE

This brief records known issues, known warnings, legacy routes, deprecated flows and migration guardrails before Pro migration.

The goal is to prevent future chats or developers from accidentally treating older routes as the current system direction.

This brief does not request code cleanup before migration.

It is a control document.

---

# 2. CURRENT BASELINE

The current migration baseline is:

- Business Inputs
- Business Truth
- Business Review
- Quote Checker
- Developer / Trace

The current preferred user-facing flow is:

1. /business-overview
2. /model-readiness
3. /business-modelling
4. /quote-checker

Developer / trace routes:

- /ai-business-state
- /calculation-trace

---

# 3. LEGACY / OLDER ROUTES

The following routes remain in the app but should not drive new build decisions unless explicitly selected:

- /recovery-summary
- /cost-allocation
- /rate-builder
- /recovery-outcome (legacy compatibility route for /business-outcome)
- /quote-engine

These routes may still contain useful earlier work.

They should not be deleted before migration.

They should not be treated as the primary v5.0 flow.

---

# 4. CURRENT PREFERRED REPLACEMENTS

Older route / concept:

- Recovery Outcome

Preferred v5.0 flow:

- /business-outcome as the primary Business Outcome route
- Business Modelling
- Business Overview
- Quote Checker

Post-rename note:

- The working Recovery Outcome module has been renamed to Business Outcome in active code.
- /recovery-outcome remains as a compatibility route only.
- The current implementation still preserves the existing Recovery Summary -> Cost Allocation -> Business Outcome source chain.
- Full S16 Business Outcome contract/source reconciliation remains future work.

Older route / concept:

- Quote Engine

Preferred v5.0 route:

- /quote-checker

Older route / concept:

- Calculation inspection mixed into business truth navigation

Preferred v5.0 placement:

- Developer / Trace group

Older route / concept:

- Recovery Chain as main downstream flow

Preferred v5.0 flow:

- Business Review group

---

# 5. KNOWN WARNINGS

Known warning 1:

MODULE_TYPELESS_PACKAGE_JSON warning from:

- lib/calculation-trace/calculationTracePilotMap.js

This warning appears when running:

node scripts\check-calculation-trace-integrity.mjs

Do not add "type": "module" to package.json as part of this migration baseline.

Known warning 2:

Turbopack NFT warning from:

- next.config.ts
- app/api/pnl-import/route.js

This warning may appear during:

npm run build

It is known and unrelated to S03-S12.

Do not treat it as a migration failure unless build behaviour changes.

---

# 6. KNOWN STRUCTURAL NOTES

The current app uses:

- Next.js App Router
- React client components
- localStorage-driven state in existing modules
- output contracts for downstream consumption
- read-only supporting layers for readiness, export, dashboard and quote checking

Some future API work may require separating client-only state from server-safe export structures.

This is deferred until after migration.

---

# 7. DO NOT CLEAN UP BEFORE MIGRATION

Before migration, do not:

- delete legacy routes
- remove Recovery Chain pages
- remove Quote Engine
- refactor source modules
- refactor Calculation Trace
- add "type": "module" to package.json
- convert localStorage state to database state
- add API export endpoint
- add saved quote scenarios
- add PDF quote export
- add AI quote review
- force / to redirect to /business-overview

Migration priority is preserving the current working baseline.

---

# 8. SAFE POST-MIGRATION CLEANUP OPTIONS

After migration, possible cleanup stages:

- review whether Recovery Chain should be archived
- review whether Quote Engine should be replaced by Quote Checker
- decide whether Business Overview should become /
- add role-based visibility for Developer / Trace routes
- add route descriptions/tooltips
- refine mobile navigation
- polish Business Overview UI
- polish Quote Checker UI

These are not required before migration.

---

# 9. VALIDATION COMMANDS

Use before and after migration:

node scripts\check-calculation-trace-integrity.mjs

npm run build

git status

Expected:

- Calculation Trace integrity passes
- npm run build passes
- git status clean

Known warnings are acceptable if validation still passes.

---

# 10. MIGRATION GUARDRAIL

If a future chat or developer is unsure which route is current, use this rule:

Current v5.0 flow:

- /business-overview
- /model-readiness
- /business-modelling
- /quote-checker

Developer / trace support:

- /ai-business-state
- /calculation-trace

Legacy routes:

- keep available
- do not expand unless explicitly selected
- do not delete before migration

---

# 11. FINAL LOCK STATEMENT

Known issues and deprecated route control are documented for migration.

No cleanup code is required before Pro migration.

Legacy routes remain available but should not drive the v5.0 handover baseline.

---

# 12. FINAL PRINCIPLE

Preserve the working baseline before migration.

Do not clean up old routes until the new baseline is safely migrated.

---

# 13. ADDITIONAL KNOWN CONTRACT REVIEW ITEMS

The following items are safe to carry forward as known issues. They do not block migration, but they must not be lost.

## 13.1 required_labour_burden_rate - unclassified Cost Summary field

The field required_labour_burden_rate surfaced during Calculation Trace source verification.

It exists in costSummaryCalculations.js per the verification register, but it is not clearly classified in the locked Cost Summary brief.

Required follow-up:

- classify as approved output, compatibility alias, helper field, or rejected/deprecated field
- record owner
- record downstream use if any
- update contract documentation if retained

This is not a migration blocker.

## 13.2 required_revenue - trace known-gap cross-reference

required_revenue is a documented compatibility alias for total_cost_burden.

It should be added to the Calculation Trace pilot known-gaps list for consistency with the existing total_business_cost_annual alias note.

Required follow-up:

- cross-reference required_revenue in Calculation Trace known gaps
- confirm no duplicate calculation ownership
- preserve compatibility where current consumers rely on it

This is not a migration blocker.

## 13.3 selected_model versus target_model relationship

S06 AI-readable Business State Export exposes selected_model.

S05 Business Modelling exposes target_model.

The relationship between these fields must be clarified.

Required follow-up:

- confirm whether selected_model is an alias, fallback, or separate field
- confirm what S08 Quote Checker reads for target margin and required recovery
- document whether Quote Checker margin/recovery warning logic currently fires or is deferred because target-specific fields are not yet populated

This is not a migration blocker, but it affects future confidence in quote alignment warnings.

## 13.4 Revenue Summary contract hardening

Revenue Summary exists as an implemented route and supporting module.

A locked brief has now been added:

- S15_REVENUE_SUMMARY_LOCKED_BRIEF_v5.0.txt

Remaining follow-up:

- inspect useRevenueSummary.js
- confirm the exact formal output_contract shape
- add Revenue Summary variables to the variable audit register
- map persistent fields during the S14 database-readiness process

This is not a migration blocker now that the route/module has been confirmed and documented.
